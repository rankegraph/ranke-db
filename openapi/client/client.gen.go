// Package client provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package client

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	"github.com/oapi-codegen/runtime"
)

// Defines values for ExecutionReport.
const (
	Debug ExecutionReport = "debug"
	Info  ExecutionReport = "info"
	Trace ExecutionReport = "trace"
)

// Valid indicates whether the value is a known member of the ExecutionReport enum.
func (e ExecutionReport) Valid() bool {
	switch e {
	case Debug:
		return true
	case Info:
		return true
	case Trace:
		return true
	default:
		return false
	}
}

// Defines values for OrderKeyCompare.
const (
	Lexical  OrderKeyCompare = "lexical"
	Numeric  OrderKeyCompare = "numeric"
	Temporal OrderKeyCompare = "temporal"
)

// Valid indicates whether the value is a known member of the OrderKeyCompare enum.
func (e OrderKeyCompare) Valid() bool {
	switch e {
	case Lexical:
		return true
	case Numeric:
		return true
	case Temporal:
		return true
	default:
		return false
	}
}

// Defines values for OrderKeyDir.
const (
	Asc  OrderKeyDir = "asc"
	Desc OrderKeyDir = "desc"
)

// Valid indicates whether the value is a known member of the OrderKeyDir enum.
func (e OrderKeyDir) Valid() bool {
	switch e {
	case Asc:
		return true
	case Desc:
		return true
	default:
		return false
	}
}

// Defines values for OutputDetail.
const (
	OutputDetailClaims   OutputDetail = "claims"
	OutputDetailEnvelope OutputDetail = "envelope"
	OutputDetailId       OutputDetail = "id"
)

// Valid indicates whether the value is a known member of the OutputDetail enum.
func (e OutputDetail) Valid() bool {
	switch e {
	case OutputDetailClaims:
		return true
	case OutputDetailEnvelope:
		return true
	case OutputDetailId:
		return true
	default:
		return false
	}
}

// Defines values for OutputEncoding.
const (
	Cbor OutputEncoding = "cbor"
	Json OutputEncoding = "json"
)

// Valid indicates whether the value is a known member of the OutputEncoding enum.
func (e OutputEncoding) Valid() bool {
	switch e {
	case Cbor:
		return true
	case Json:
		return true
	default:
		return false
	}
}

// Defines values for OutputForm.
const (
	Materialized OutputForm = "materialized"
	Original     OutputForm = "original"
)

// Valid indicates whether the value is a known member of the OutputForm enum.
func (e OutputForm) Valid() bool {
	switch e {
	case Materialized:
		return true
	case Original:
		return true
	default:
		return false
	}
}

// Defines values for OutputShape.
const (
	Path   OutputShape = "path"
	Single OutputShape = "single"
)

// Valid indicates whether the value is a known member of the OutputShape enum.
func (e OutputShape) Valid() bool {
	switch e {
	case Path:
		return true
	case Single:
		return true
	default:
		return false
	}
}

// Defines values for OutputContentOverflow.
const (
	Cutoff OutputContentOverflow = "cutoff"
	Omit   OutputContentOverflow = "omit"
)

// Valid indicates whether the value is a known member of the OutputContentOverflow enum.
func (e OutputContentOverflow) Valid() bool {
	switch e {
	case Cutoff:
		return true
	case Omit:
		return true
	default:
		return false
	}
}

// Defines values for PathStepDir.
const (
	Connections PathStepDir = "connections"
	Provenance  PathStepDir = "provenance"
	Uses        PathStepDir = "uses"
)

// Valid indicates whether the value is a known member of the PathStepDir enum.
func (e PathStepDir) Valid() bool {
	switch e {
	case Connections:
		return true
	case Provenance:
		return true
	case Uses:
		return true
	default:
		return false
	}
}

// Defines values for VerificationConfigDepth.
const (
	Completeness      VerificationConfigDepth = "completeness"
	FullContent       VerificationConfigDepth = "full-content"
	RecordCorrectness VerificationConfigDepth = "record-correctness"
)

// Valid indicates whether the value is a known member of the VerificationConfigDepth enum.
func (e VerificationConfigDepth) Valid() bool {
	switch e {
	case Completeness:
		return true
	case FullContent:
		return true
	case RecordCorrectness:
		return true
	default:
		return false
	}
}

// Defines values for VerificationFailureMode.
const (
	CorruptBytes   VerificationFailureMode = "corrupt-bytes"
	InvalidContent VerificationFailureMode = "invalid-content"
)

// Valid indicates whether the value is a known member of the VerificationFailureMode enum.
func (e VerificationFailureMode) Valid() bool {
	switch e {
	case CorruptBytes:
		return true
	case InvalidContent:
		return true
	default:
		return false
	}
}

// Defines values for VerificationReportStatus.
const (
	VerificationReportStatusComplete VerificationReportStatus = "complete"
	VerificationReportStatusError    VerificationReportStatus = "error"
	VerificationReportStatusRunning  VerificationReportStatus = "running"
	VerificationReportStatusStopped  VerificationReportStatus = "stopped"
)

// Valid indicates whether the value is a known member of the VerificationReportStatus enum.
func (e VerificationReportStatus) Valid() bool {
	switch e {
	case VerificationReportStatusComplete:
		return true
	case VerificationReportStatusError:
		return true
	case VerificationReportStatusRunning:
		return true
	case VerificationReportStatusStopped:
		return true
	default:
		return false
	}
}

// ArchiveInfo defines model for ArchiveInfo.
type ArchiveInfo struct {
	// Branches How many branches the table holds.
	Branches int `json:"branches"`

	// Head The branch-table head id — the root of every `$archive`-scoped read, and the
	// only place this contract reports it.
	Head string `json:"head"`

	// Height The branch-table head claim's generation number.
	Height int64 `json:"height"`

	// UpdatedAt The branch-table head's `created_at` — when the archive last moved.
	UpdatedAt time.Time `json:"updatedAt"`
}

// BranchEntry defines model for BranchEntry.
type BranchEntry struct {
	// Head That branch's current head claim id.
	Head string `json:"head"`

	// Name The branch name, as the branch table holds it.
	Name string `json:"name"`
}

// BranchHead defines model for BranchHead.
type BranchHead struct {
	// Head The content-addressed head claim id.
	Head string `json:"head"`
}

// BranchInfo defines model for BranchInfo.
type BranchInfo struct {
	// Head The content-addressed head claim id.
	Head string `json:"head"`

	// Height The head claim's generation number (§4.1) — 0 on an initial node, else
	// 1 + max over what it references. The depth of what the branch points at.
	Height int64 `json:"height"`

	// Name The branch name, as the branch table holds it.
	Name string `json:"name"`

	// UpdatedAt The head claim's `created_at` — when the branch last moved. Soft: a
	// contributor writes it, so it is not the witnessed merge time.
	UpdatedAt time.Time `json:"updatedAt"`
}

// BranchList defines model for BranchList.
type BranchList struct {
	// Branches Every branch the branch table holds, from one archive snapshot — so the
	// heads are consistent with each other. Empty on an archive with no branches.
	Branches []BranchEntry `json:"branches"`
}

// Comparison One operator applied to one field. eq, ne, lt, le, gt and ge take a value, in a set, glob a shell-style wildcard. Exactly one is present.
type Comparison struct {
	// Eq A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Eq *Value `json:"eq,omitempty"`

	// Ge A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Ge *Value `json:"ge,omitempty"`

	// Glob Shell-style wildcard.
	Glob *string `json:"glob,omitempty"`

	// Gt A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Gt *Value `json:"gt,omitempty"`

	// In Set membership.
	In *[]Value `json:"in,omitempty"`

	// Le A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Le *Value `json:"le,omitempty"`

	// Lt A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Lt *Value `json:"lt,omitempty"`

	// Ne A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
	Ne *Value `json:"ne,omitempty"`
}

// ContributionResult The outcome of a contribution — the new branch-table head and the appended claim ids.
type ContributionResult struct {
	// Head The new branch-table head id after the merge.
	Head string `json:"head"`

	// Ids Content-addressed ids of the contributed claims, in order.
	Ids []string `json:"ids"`
}

// DevClock The dev sequencer's clock after the request — the later of what was asked
// and what it already held.
type DevClock struct {
	Time time.Time `json:"time"`
}

// DevClockAdvance Requests the dev sequencer's clock advance to (at least) this instant. A
// request older than the clock's current position is accepted as a no-op —
// the clock only ever moves forward, since a merge's witnessed time
// regressing would break every guarantee built on it.
type DevClockAdvance struct {
	// Time The instant to advance to.
	Time time.Time `json:"time"`
}

// Duration A duration as a decimal sequence with unit suffixes — ns, us, ms, s, m, h — e.g. 5s or 1m30s. The bare 0 means unbounded.
type Duration = string

// Error defines model for Error.
type Error struct {
	// Code Machine-readable failure category, stable across releases — one of
	// unauthenticated, forbidden, not_found, conflict, busy, invalid,
	// unimplemented, internal. Clients branch on this, not on the message.
	Code string `json:"code"`

	// Error Human-readable message. Carries no subject id, even on 403.
	Error string `json:"error"`
}

// Execution Where the query runs and how it reports on itself. These controls reach execution and diagnostics, and never the result set.
type Execution struct {
	// Layer Pins the query to one named storage or execution layer; absent, the backend chooses by capability.
	Layer *string `json:"layer,omitempty"`

	// Report Report verbosity: info gives high-level stages, debug routing and translation, trace per-claim detail. Set, and only then, the stream carries one final report record after the last element, typed distinctly from result claims (R-QREPORT).
	Report *ExecutionReport `json:"report,omitempty"`
}

// ExecutionReport Report verbosity: info gives high-level stages, debug routing and translation, trace per-claim detail. Set, and only then, the stream carries one final report record after the last element, typed distinctly from result claims (R-QREPORT).
type ExecutionReport string

// Health defines model for Health.
type Health struct {
	// Signer The contributor identity this stack signs merges with.
	//
	// Example: did:key:z6Mk...
	Signer *string `json:"signer,omitempty"`

	// Status Example: ok
	Status string `json:"status"`

	// Version The server build answering this request. A release names itself exactly;
	// a build from a checkout names its revision, marked when the tree carried
	// uncommitted changes.
	//
	//
	// Example: v1.19.2
	Version string `json:"version"`
}

// Id A claim id: id(v) = Sign(H(S(v))) for a node, id(e) = H(S(e)) for an edge, carried as multibase base32 of the self-describing payload. The pattern fixes the multibase framing; whether the payload's multihash or multikey framing parses is the implementation's check.
type Id = string

// Limit Bounds the read. A read cut short by either bound is a complete answer to the query as bounded, not an error (R-QLIMIT).
type Limit struct {
	// Results Caps the claim count; 0 is unbounded.
	Results *int `json:"results,omitempty"`

	// Time A duration as a decimal sequence with unit suffixes — ns, us, ms, s, m, h — e.g. 5s or 1m30s. The bare 0 means unbounded.
	Time *Duration `json:"time,omitempty"`
}

// Order Sort keys applied in priority order. Claims lacking a key's field sort last, and the archive's natural (created_at, id) order breaks any remaining ties, so the sort always resolves to a total order (R-QSORT).
type Order = []OrderKey

// OrderKey defines model for OrderKey.
type OrderKey struct {
	// Compare How the values compare (R-QSORT). temporal reads each value as the span of time it denotes and orders by that span's midpoint in nanoseconds, so EDTF dates and instants compare on one axis (R-QTEMPORAL).
	Compare *OrderKeyCompare `json:"compare,omitempty"`

	// Dir Sort direction (R-QSORT).
	Dir *OrderKeyDir `json:"dir,omitempty"`

	// Field The field sorted on — any field a claim carries, height included.
	Field string `json:"field"`
}

// OrderKeyCompare How the values compare (R-QSORT). temporal reads each value as the span of time it denotes and orders by that span's midpoint in nanoseconds, so EDTF dates and instants compare on one axis (R-QTEMPORAL).
type OrderKeyCompare string

// OrderKeyDir Sort direction (R-QSORT).
type OrderKeyDir string

// Output Shapes each result along orthogonal axes. detail: claims with form: original and encoding: cbor reproduces the canonical serialization S(v) a claim's id is computed over, and is the only output form directly verifiable against that id (R-QCANON).
type Output struct {
	// Content Inline content per claim. Absent, no content is inlined (R-QCONTENT).
	Content *OutputContent `json:"content,omitempty"`

	// Detail What each element carries: id (the id alone), claims (a record the engine assembles, shaped by form and content), or envelope (the stored bytes copied out whole, the only output a client can hash against an id; form and content do not apply, and both form: materialized and encoding: json are rejected with it). Under shape: path it applies to every claim in the route (R-QDETAIL, R-QCANON).
	Detail *OutputDetail `json:"detail,omitempty"`

	// Encoding json is text with content base64-encoded, cbor is binary; the same information either way (R-QENCODING).
	Encoding *OutputEncoding `json:"encoding,omitempty"`

	// Form Which field values a claim carries: original as written, a diff-overlaid claim's delta; materialized with any contribution/diff chain resolved over the predecessor it references, recursively to a base claim. A property of the values, hence orthogonal to detail and encoding (R-QFORM).
	Form *OutputForm `json:"form,omitempty"`

	// Shape single yields the reached endpoints, one element each; path yields routes, each running outward from the frontier claim its walk began at (R-QSHAPE).
	Shape *OutputShape `json:"shape,omitempty"`
}

// OutputDetail What each element carries: id (the id alone), claims (a record the engine assembles, shaped by form and content), or envelope (the stored bytes copied out whole, the only output a client can hash against an id; form and content do not apply, and both form: materialized and encoding: json are rejected with it). Under shape: path it applies to every claim in the route (R-QDETAIL, R-QCANON).
type OutputDetail string

// OutputEncoding json is text with content base64-encoded, cbor is binary; the same information either way (R-QENCODING).
type OutputEncoding string

// OutputForm Which field values a claim carries: original as written, a diff-overlaid claim's delta; materialized with any contribution/diff chain resolved over the predecessor it references, recursively to a base claim. A property of the values, hence orthogonal to detail and encoding (R-QFORM).
type OutputForm string

// OutputShape single yields the reached endpoints, one element each; path yields routes, each running outward from the frontier claim its walk began at (R-QSHAPE).
type OutputShape string

// OutputContent Inline content per claim. Absent, no content is inlined (R-QCONTENT).
type OutputContent struct {
	// Max Cap in bytes on the content inlined per claim; 0 inlines every claim's content in full.
	Max int `json:"max"`

	// Overflow What becomes of content past the cap: cutoff inlines the bytes up to it, omit inlines whole values only. Absent, omit. A claim keeps every field it carries either way (R-QCONTENT).
	Overflow *OutputContentOverflow `json:"overflow,omitempty"`
}

// OutputContentOverflow What becomes of content past the cap: cutoff inlines the bytes up to it, omit inlines whole values only. Absent, omit. A claim keeps every field it carries either way (R-QCONTENT).
type OutputContentOverflow string

// PathStep One section of a path. edges bounds the walk: every hop must follow an edge whose type is listed. nodes bounds the answer: a step yields a claim only when its node's type is listed, whatever the nodes it crossed to reach it. min and max bound the hops. A min above a bounded max is refused by the implementation — a JSON Schema cannot compare two sibling values (R-QSTEPS).
type PathStep struct {
	// Dir provenance follows references outward, uses runs to the claims that cite this one, connections either way. Absent, provenance.
	Dir *PathStepDir `json:"dir,omitempty"`

	// Edges Edge types every hop must match.
	Edges *[]TypeGlob `json:"edges,omitempty"`

	// Max Most hops. 0, or absent, leaves the step unbounded: a step of at most zero hops would move nothing, so that reading has no use.
	Max *int `json:"max,omitempty"`

	// Min Fewest hops. Absent, 1 — a step moves at least one hop. 0 also yields the starting set, carrying the frontier through alongside what lies beyond it.
	Min *int `json:"min,omitempty"`

	// Nodes Node types the step may yield.
	Nodes *[]TypeGlob `json:"nodes,omitempty"`
}

// PathStepDir provenance follows references outward, uses runs to the claims that cite this one, connections either way. Absent, provenance.
type PathStepDir string

// Query A read, evaluated in a fixed logical order: select generates the result set, where filters it, order sorts it, limit truncates it, output shapes and encodes each surviving claim (R-QEVAL).
type Query struct {
	// Execution Where the query runs and how it reports on itself. These controls reach execution and diagnostics, and never the result set.
	Execution *Execution `json:"execution,omitempty"`

	// Limit Bounds the read. A read cut short by either bound is a complete answer to the query as bounded, not an error (R-QLIMIT).
	Limit *Limit `json:"limit,omitempty"`

	// Order Sort keys applied in priority order. Claims lacking a key's field sort last, and the archive's natural (created_at, id) order breaks any remaining ties, so the sort always resolves to a total order (R-QSORT).
	Order *Order `json:"order,omitempty"`

	// Output Shapes each result along orthogonal axes. detail: claims with form: original and encoding: cbor reproduces the canonical serialization S(v) a claim's id is computed over, and is the only output form directly verifiable against that id (R-QCANON).
	Output *Output `json:"output,omitempty"`

	// Select A generator in four independent parts: branch is the scope, head the closure read, claim where the walk starts, path the traversal. Scope and start are independent because a walk runs both ways — a uses step reaches the claims that cite the current one, which lie above it, so the closure decides a reverse step's answer.
	Select Select `json:"select"`

	// Where A boolean tree. Each node is exactly one of the and / or / not combinators over sub-trees, or a leaf naming a field and its test. Within a where, or is boolean; across generators it unions whole result sets. A leaf may name any field a claim carries, height included (V-HEIGHT).
	Where *Where `json:"where,omitempty"`
}

// Select A generator in four independent parts: branch is the scope, head the closure read, claim where the walk starts, path the traversal. Scope and start are independent because a walk runs both ways — a uses step reaches the claims that cite the current one, which lie above it, so the closure decides a reverse step's answer.
type Select struct {
	// Branch The mandatory scope, and every scope names a graph: a branch name confines to that branch, $archive to the whole Ranke-Archive, $universe applies no confinement and is privileged. An empty value is refused (R-QSCOPE).
	Branch string `json:"branch"`

	// Claim A claim id: id(v) = Sign(H(S(v))) for a node, id(e) = H(S(e)) for an edge, carried as multibase base32 of the self-describing payload. The pattern fixes the multibase framing; whether the payload's multihash or multikey framing parses is the implementation's check.
	Claim *Id `json:"claim,omitempty"`

	// Head A claim id: id(v) = Sign(H(S(v))) for a node, id(e) = H(S(e)) for an edge, carried as multibase base32 of the self-describing payload. The pattern fixes the multibase framing; whether the payload's multihash or multikey framing parses is the implementation's check.
	Head *Id `json:"head,omitempty"`

	// Path The traversal: a sequence of steps over frontiers, each frontier a set of claims. Each step's yield is the frontier the next starts from, and the no-repeat rule holds within a step and resets at each boundary, so membership is all a frontier carries (R-QFRONTIER). Absent, the generator returns the full outward closure of the frontier (R-QSTEPS).
	Path *[]PathStep `json:"path,omitempty"`
}

// StorageLayer defines model for StorageLayer.
type StorageLayer struct {
	Name string `json:"name"`

	// Type Adapter type (e.g. memory, filesystem, s3, redis, neo4j).
	Type string `json:"type"`
}

// StorageLayerList defines model for StorageLayerList.
type StorageLayerList struct {
	// Layers Read-through tiers, top (cache) to bottom (authoritative).
	Layers []StorageLayer `json:"layers"`
}

// Subject defines model for Subject.
type Subject struct {
	// Account The account the credential resolved to.
	//
	// Example: webapp
	Account string `json:"account"`

	// Caveats Attenuations carried by this credential, narrowing the grants above.
	// Every caveat must allow a request for it to pass. Empty where the
	// credential carries none.
	//
	//
	// Example: ["R foo_bar"]
	Caveats []string `json:"caveats"`

	// Grants The account's grants, each a `RIGHTS glob` spec as the configuration
	// states it. An account holding none reports an empty array.
	//
	//
	// Example: ["CR foo_*","R $archive"]
	Grants []string `json:"grants"`
}

// TypeGlob A glob over class/sub, e.g. derivation/* or entity/person. A leading - excludes. Exclusion decides: a type matching an excluded pattern is refused whatever the included patterns say, and a list of exclusions alone admits every other type (R-QSTEPS).
type TypeGlob = string

// Value A value a comparison tests against. Where it tests a time it MUST be a V-TIME timestamp or an EDTF Level 1 value, and anything else is rejected rather than coerced (R-QTIMEOP); otherwise how two values compare is the engine's.
type Value = interface{}

// VerificationConfig Parameters for a verification run — the same shape whether declared in the
// stack config (scheduled) or posted ad-hoc. Depths are those of
// core-verification.
type VerificationConfig struct {
	// Closure Root of the closure to verify — a branch name or a claim id.
	Closure string `json:"closure"`

	// ContentThreshold For full-content, the max content size in bytes to re-read and re-hash
	// per claim; larger content is skipped this run. Omit to verify all content.
	ContentThreshold *int `json:"contentThreshold,omitempty"`

	// Depth completeness (a `has` sweep), record-correctness (recanonicalise and
	// recheck the id-chain and signatures), or full-content (also re-hash blobs).
	Depth *VerificationConfigDepth `json:"depth,omitempty"`

	// Layer Storage layer (by name) to read directly. Omit to read the composed
	// read-through view — which may mask the loss of an object on a deeper
	// layer, so name a layer to verify it without blind spots.
	Layer *string `json:"layer,omitempty"`
}

// VerificationConfigDepth completeness (a `has` sweep), record-correctness (recanonicalise and
// recheck the id-chain and signatures), or full-content (also re-hash blobs).
type VerificationConfigDepth string

// VerificationFailure defines model for VerificationFailure.
type VerificationFailure struct {
	Detail *string `json:"detail,omitempty"`

	// Id The claim or object id that failed.
	Id string `json:"id"`

	// Layer The layer where the failure was observed.
	Layer string `json:"layer"`

	// Mode corrupt-bytes — stored bytes don't match their hash (storage rot/loss;
	// self-heals via read-through if a deeper layer is intact).
	// invalid-content — the claim itself doesn't validate (e.g. bad signature);
	// unrepairable.
	Mode VerificationFailureMode `json:"mode"`
}

// VerificationFailureMode corrupt-bytes — stored bytes don't match their hash (storage rot/loss;
// self-heals via read-through if a deeper layer is intact).
// invalid-content — the claim itself doesn't validate (e.g. bad signature);
// unrepairable.
type VerificationFailureMode string

// VerificationReport A point-in-time record of a verification run; embeds the config that produced it.
type VerificationReport struct {
	// BytesRead Content bytes re-read so far; advances while `status` is `running`.
	BytesRead *int `json:"bytesRead,omitempty"`

	// ClaimsChecked Claims checked so far; advances while `status` is `running`.
	ClaimsChecked *int       `json:"claimsChecked,omitempty"`
	CompletedAt   *time.Time `json:"completedAt,omitempty"`

	// Config Parameters for a verification run — the same shape whether declared in the
	// stack config (scheduled) or posted ad-hoc. Depths are those of
	// core-verification.
	Config VerificationConfig `json:"config"`

	// Failures The failures found, accumulating while `status` is `running`. Empty (with
	// `ok: true`) for an intact closure; a healthy archive verifies with none.
	Failures *[]VerificationFailure `json:"failures,omitempty"`

	// Head The head id the run verified — the closure it pinned at start. When
	// `config.closure` names a branch, this is the branch's head at start time;
	// the report stays fixed to it even as the branch head moves on.
	Head string `json:"head"`
	Id   string `json:"id"`

	// Ok True when no failures were found in this run.
	Ok        bool      `json:"ok"`
	StartedAt time.Time `json:"startedAt"`

	// Status running (in progress), complete (finished on its own), stopped (cancelled
	// by an operator via the cancel action — partial findings kept), or error
	// (the run itself failed).
	Status VerificationReportStatus `json:"status"`
}

// VerificationReportStatus running (in progress), complete (finished on its own), stopped (cancelled
// by an operator via the cancel action — partial findings kept), or error
// (the run itself failed).
type VerificationReportStatus string

// VerificationReportList defines model for VerificationReportList.
type VerificationReportList struct {
	Reports []VerificationReport `json:"reports"`
}

// Where A boolean tree. Each node is exactly one of the and / or / not combinators over sub-trees, or a leaf naming a field and its test. Within a where, or is boolean; across generators it unions whole result sets. A leaf may name any field a claim carries, height included (V-HEIGHT).
type Where struct {
	union json.RawMessage
}

// Where0 defines model for Where.0.
type Where0 struct {
	And []Where `json:"and"`
}

// Where1 defines model for Where.1.
type Where1 struct {
	Or []Where `json:"or"`
}

// Where2 defines model for Where.2.
type Where2 struct {
	// Not A boolean tree. Each node is exactly one of the and / or / not combinators over sub-trees, or a leaf naming a field and its test. Within a where, or is boolean; across generators it unions whole result sets. A leaf may name any field a claim carries, height included (V-HEIGHT).
	Not Where `json:"not"`
}

// Where3 A leaf: one field, one comparison.
type Where3 struct {
	// Field The field tested — any field a claim carries, height included.
	Field string `json:"field"`

	// Test One operator applied to one field. eq, ne, lt, le, gt and ge take a value, in a set, glob a shell-style wildcard. Exactly one is present.
	Test Comparison `json:"test"`
}

// BranchName defines model for BranchName.
type BranchName = string

// ClaimId defines model for ClaimId.
type ClaimId = string

// BadRequest defines model for BadRequest.
type BadRequest = Error

// Conflict defines model for Conflict.
type Conflict = Error

// Forbidden defines model for Forbidden.
type Forbidden = Error

// NotFound defines model for NotFound.
type NotFound = Error

// NotImplemented defines model for NotImplemented.
type NotImplemented = Error

// Unauthorized defines model for Unauthorized.
type Unauthorized = Error

// AdvanceDevClockJSONRequestBody defines body for AdvanceDevClock for application/json ContentType.
type AdvanceDevClockJSONRequestBody = DevClockAdvance

// QueryJSONRequestBody defines body for Query for application/json ContentType.
type QueryJSONRequestBody = Query

// StartVerificationJSONRequestBody defines body for StartVerification for application/json ContentType.
type StartVerificationJSONRequestBody = VerificationConfig

// AsWhere0 returns the union data inside the Where as a Where0
func (t Where) AsWhere0() (Where0, error) {
	var body Where0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromWhere0 overwrites any union data inside the Where as the provided Where0
func (t *Where) FromWhere0(v Where0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeWhere0 performs a merge with any union data inside the Where, using the provided Where0
func (t *Where) MergeWhere0(v Where0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsWhere1 returns the union data inside the Where as a Where1
func (t Where) AsWhere1() (Where1, error) {
	var body Where1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromWhere1 overwrites any union data inside the Where as the provided Where1
func (t *Where) FromWhere1(v Where1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeWhere1 performs a merge with any union data inside the Where, using the provided Where1
func (t *Where) MergeWhere1(v Where1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsWhere2 returns the union data inside the Where as a Where2
func (t Where) AsWhere2() (Where2, error) {
	var body Where2
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromWhere2 overwrites any union data inside the Where as the provided Where2
func (t *Where) FromWhere2(v Where2) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeWhere2 performs a merge with any union data inside the Where, using the provided Where2
func (t *Where) MergeWhere2(v Where2) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsWhere3 returns the union data inside the Where as a Where3
func (t Where) AsWhere3() (Where3, error) {
	var body Where3
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromWhere3 overwrites any union data inside the Where as the provided Where3
func (t *Where) FromWhere3(v Where3) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeWhere3 performs a merge with any union data inside the Where, using the provided Where3
func (t *Where) MergeWhere3(v Where3) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t Where) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *Where) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// GetArchiveClaim Fetch a claim within the archive head's closure
	//
	// Returns claim `{id}` as its signed CBOR bytes if it lies in the closure of the
	// **whole Ranke-Archive** — the current branch-table head — whichever branch holds
	// it. A client reaches a claim without naming the branch it is on; a claim outside
	// that closure returns `404`.
	//
	// This collection reads the `$archive` scope: the same scope a RankeQL body names
	// as `select.branch: "$archive"`, and the same target a grant is written against.
	// It requires the **R** right on `$archive`.
	//
	// Immutably cacheable by id.
	//
	// Corresponds with GET /archive/claims/{id} (the `GetArchiveClaim` operationId).
	GetArchiveClaim(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetArchiveClaimContent Fetch the content of a claim within the archive head's closure
	//
	// Streams the content of claim `{id}` if it lies in the archive head's closure,
	// across every branch (same closure guarantee as the claim itself; outside it or
	// unknown → `404`). Reads the `$archive` scope and requires **R** on `$archive`.
	// Content is addressed by the claim; whether the bytes are inline or a separate
	// blob is hidden. Immutably cacheable by the claim id.
	//
	// Corresponds with GET /archive/claims/{id}/content (the `GetArchiveClaimContent` operationId).
	GetArchiveClaimContent(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetArchiveInfo What is known about the archive
	//
	// Reports the Ranke-Archive as a whole: the **branch-table head** — the id every
	// archive-scoped read is rooted at — with its height and when it last moved, and how
	// many branches the table holds.
	//
	// This is the only route that reports the branch-table head, which is what a client
	// needs to name the `$archive` scope in a query or a grant.
	//
	// Reads the `$archive` scope and requires the **R** right on `$archive`. Cacheable
	// **with revalidation**: the head advances on every contribution.
	//
	// Corresponds with GET /archive/info (the `GetArchiveInfo` operationId).
	GetArchiveInfo(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBranches List the branch table's branches
	//
	// Returns every branch the branch table holds, each with its name and current
	// head id. Reachable **without knowing any branch name**, so a client discovers
	// what it may address before using the routes that take one; an archive with no
	// branches yet answers with an empty list.
	//
	// Requires the **R** right on the reserved `$branches` target, which is what
	// core-access grants as "enumerates the table".
	//
	// Cacheable **with revalidation** (weak `ETag`, `Cache-Control: no-cache`): every
	// head in the list moves as its branch is contributed to. The listing is answered
	// from **one archive snapshot**, so the heads are consistent with each other.
	//
	// Corresponds with GET /branches (the `ListBranches` operationId).
	ListBranches(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBranchClaim Fetch a claim within a branch's closure
	//
	// Returns claim `{id}` as its signed CBOR bytes, **only if it lies in branch
	// `{branch}`'s closure**. A claim that is superseded, contradicted, or otherwise
	// outside the closure returns `404`, indistinguishable from one that does not
	// exist. Requires the **R** right on that branch.
	//
	// `{branch}` names an **ordinary branch**. A reserved scope name supplied here
	// names a branch that does not exist and is answered as `404`; each scope has
	// exactly one route (`/archive/…`, `/universe/…`).
	//
	// Immutably **cacheable** by id (strong `ETag`, `Cache-Control: public,
	// immutable`): the id content-addresses the bytes, so they never change.
	//
	// Corresponds with GET /branches/{branch}/claims/{id} (the `GetBranchClaim` operationId).
	GetBranchClaim(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBranchClaimContent Fetch the content of a claim within a branch's closure
	//
	// Streams the content of claim `{id}` — **only if it lies in branch `{branch}`'s
	// closure** (same closure guarantee as the claim itself; out-of-closure or
	// unknown → `404`). Requires the **R** right on that branch. Content is addressed
	// by the claim that holds it, not by a raw hash: the server resolves whether the
	// bytes live inline in the claim or in a separate blob, so the client can't tell
	// and doesn't need to — and the read stays scoped as the route is scoped.
	// Immutably **cacheable** by the claim id.
	//
	// Corresponds with GET /branches/{branch}/claims/{id}/content (the `GetBranchClaimContent` operationId).
	GetBranchClaimContent(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBranchHead Current head id of a branch
	//
	// Returns the branch's current head id — a moving target (it advances on every
	// contribution). Requires the **R** right on that branch. Cacheable **with
	// revalidation** (weak `ETag`, `Cache-Control: no-cache`): a conditional request
	// is cheap when the head has not moved. To inspect the head claim itself, fetch it
	// via `GET /branches/{branch}/claims/{id}`.
	//
	// Corresponds with GET /branches/{branch}/head (the `GetBranchHead` operationId).
	GetBranchHead(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBranchInfo What is known about a branch
	//
	// Reports a branch beyond its head id: the head's **height** — the generation number
	// of the branch's newest claim, so the depth of what it points at — and **when it last
	// moved**, which is the head claim's `created_at`.
	//
	// Everything here comes from the head claim, so it costs one claim read. A claim count
	// is deliberately absent: counting a branch's claims is a walk of its closure, which is
	// a query (`POST /query`) and not a field.
	//
	// Requires the **R** right on the branch. Cacheable **with revalidation** — every
	// field moves when the branch does.
	//
	// Corresponds with GET /branches/{branch}/info (the `GetBranchInfo` operationId).
	GetBranchInfo(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ContributeWithBody Contribute signed claims (atomic)
	//
	// Contributes one or more signed claims to a branch as a single **atomic**
	// merge — all are absorbed under one new branch-table head, or none are.
	// Content-addressed and therefore **idempotent**: re-contributing yields the same
	// ids with no duplicates.
	//
	// The body is ranke's contribution stream: a **CBOR sequence** (RFC 8742) of
	// records, self-framing so a contribution of any size streams.
	//
	//   - `[2, ["<branch>", …]]` — **first**, the branches this contribution touches
	//   - `[0, <id>, <claim bytes>, "<branch>"]` — a claim, as the canonical CBOR its
	//     contributor signed, and the branch it joins
	//   - `[1, <hash>, <content bytes>]` — externalized content, which lives in the
	//     Universe unbranched and so names no branch
	//
	// The header comes first so the **C** right is settled on every declared branch
	// before any of the body is read; a claim naming an undeclared branch is refused, so
	// the declaration binds. One contribution may therefore advance **several branches**,
	// and an unauthorized one is answered without its payload being read.
	//
	// Payloads are CBOR byte strings carried through untouched: a claim's id is a
	// signature over exactly those bytes, so the server stores what it was sent. The id
	// is explicit because it cannot be derived — `Sign(H(S(node)))` is made with the
	// contributor's key, and the canonical encoding carries only the node. Content is
	// checked against the hash addressing it.
	//
	// Send every claim the closure needs that the archive may not hold yet — a
	// first-time contributor includes its own `contribution/contributor` claim, since
	// everything it signs references it.
	//
	// The sequencer mints the `contribution/branches` branch-table claim recording the
	// merge (paper 02 §Sequencer). On success the new branch-table head id and the
	// contributed claim ids are returned.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /contribute (the `Contribute` operationId).
	ContributeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AdvanceDevClockWithBody Advance the dev sequencer's clock
	//
	// Available **only** when the stack was launched with `--dev` against a `dev`
	// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
	// timestamps from forward to (at least) the given instant, so a client that
	// knows its own story's schedule — a fixture generator, say — can make the
	// archive's *recorded* history track its *narrated* one, one contribution at a
	// time, rather than every merge landing at the real wall-clock moment the
	// client happened to run.
	//
	// The clock never moves backward: a request older than its current position
	// is accepted and answered with the position unchanged. Absent `--dev`, or
	// against a `concurrent` (production) Sequencer, the route is `501` — the
	// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
	AdvanceDevClockWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AdvanceDevClock Advance the dev sequencer's clock
	//
	// Available **only** when the stack was launched with `--dev` against a `dev`
	// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
	// timestamps from forward to (at least) the given instant, so a client that
	// knows its own story's schedule — a fixture generator, say — can make the
	// archive's *recorded* history track its *narrated* one, one contribution at a
	// time, rather than every merge landing at the real wall-clock moment the
	// client happened to run.
	//
	// The clock never moves backward: a request older than its current position
	// is accepted and answered with the position unchanged. Absent `--dev`, or
	// against a `concurrent` (production) Sequencer, the route is `501` — the
	// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
	AdvanceDevClock(ctx context.Context, body AdvanceDevClockJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Health Liveness and signer identity
	//
	// Reports that the stack is up and the signing/contributor identity it
	// attests merges under. Whether this requires a privileged subject is a
	// core-access decision, not part of this contract.
	//
	// Corresponds with GET /health (the `Health` operationId).
	Health(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// QueryWithBody Read the graph with a declarative query
	//
	// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
	// result, in the serialization chosen by `output.encoding`. Results carry the
	// natural `(created_at, id)` order unless `order` sorts them, with its keys
	// applied in priority order and that natural order breaking any remaining ties;
	// to page, carry the last result's order key into the next request's `where`
	// (pin `select.head` so the closure cannot shift between pages).
	//
	// When `execution.report` names a verbosity, the **final item** in the sequence
	// is a `QueryReport` (see the schema) — typed distinctly from result items and
	// always last — carrying the execution log, timing, and whether a limit truncated
	// the read. `execution.layer` pins which storage layer runs it.
	//
	// The response media type mirrors `output.encoding`: `application/json-seq` or
	// `application/cbor-seq`. A branch named in `select` that does not exist is
	// `404`; claims outside the scope's closure simply do not appear. `$universe`
	// is the privileged unconfined read and requires `select.head`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /query (the `Query` operationId).
	QueryWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Query Read the graph with a declarative query
	//
	// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
	// result, in the serialization chosen by `output.encoding`. Results carry the
	// natural `(created_at, id)` order unless `order` sorts them, with its keys
	// applied in priority order and that natural order breaking any remaining ties;
	// to page, carry the last result's order key into the next request's `where`
	// (pin `select.head` so the closure cannot shift between pages).
	//
	// When `execution.report` names a verbosity, the **final item** in the sequence
	// is a `QueryReport` (see the schema) — typed distinctly from result items and
	// always last — carrying the execution log, timing, and whether a limit truncated
	// the read. `execution.layer` pins which storage layer runs it.
	//
	// The response media type mirrors `output.encoding`: `application/json-seq` or
	// `application/cbor-seq`. A branch named in `select` that does not exist is
	// `404`; claims outside the scope's closure simply do not appear. `$universe`
	// is the privileged unconfined read and requires `select.head`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /query (the `Query` operationId).
	Query(ctx context.Context, body QueryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListStorageLayers List storage layers
	//
	// Lists the stack's storage layers (read-through tiers) by **name and type
	// only** — never connection details or secrets. Naming layers is what lets a
	// verification run target one directly (a read-through view can mask the loss
	// of an object on a deeper layer).
	//
	// Corresponds with GET /system/layers (the `ListStorageLayers` operationId).
	ListStorageLayers(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListVerifications List verification runs
	//
	// Verification runs across the whole stack, newest first. Each report is a
	// **point-in-time record**: a layer repaired externally shows clean in a later
	// run, so reports accumulate rather than overwrite — until explicitly removed
	// with `DELETE /system/verifications/{reportId}`.
	//
	// The API lives under `/system/` because a verification run is a ranke-db
	// extension beyond the paper's surface rather than archive content: it is a
	// stack-wide operational resource, rooted at a closure named in the request body
	// and not in the path. (Branch reads no longer occupy the root, so nothing here
	// is avoiding a collision.)
	//
	// Corresponds with GET /system/verifications (the `ListVerifications` operationId).
	ListVerifications(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// StartVerificationWithBody Start a verification run
	//
	// Starts a run from a `VerificationConfig`: walk the closure rooted at
	// `closure` (a branch name — resolved to its current head and **pinned** for
	// the life of the run — or a head id directly), reading the named `layer`
	// directly, re-checking each claim to the configured depth. It returns
	// *findings*, not contents, so it never leaks across closures.
	//
	// A run may take a **very long time** (hours to days over a large closure at
	// full-content depth), so it is always **asynchronous**: the call returns `202`
	// immediately with the running report and a `Location` header pointing at the
	// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
	// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
	// yields two independent point-in-time reports (runs are not deduplicated).
	//
	// Verification is resource-heavy, so the stack caps the number of runs that may
	// execute **concurrently** (configured; default 1). When that cap is already
	// reached the call returns `429` — the server never stops a run to make room. To
	// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
	// what is running and free a slot deliberately: `cancel` a run to stop it while
	// keeping its report, or `DELETE` it to remove it entirely.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /system/verifications (the `StartVerification` operationId).
	StartVerificationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// StartVerification Start a verification run
	//
	// Starts a run from a `VerificationConfig`: walk the closure rooted at
	// `closure` (a branch name — resolved to its current head and **pinned** for
	// the life of the run — or a head id directly), reading the named `layer`
	// directly, re-checking each claim to the configured depth. It returns
	// *findings*, not contents, so it never leaks across closures.
	//
	// A run may take a **very long time** (hours to days over a large closure at
	// full-content depth), so it is always **asynchronous**: the call returns `202`
	// immediately with the running report and a `Location` header pointing at the
	// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
	// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
	// yields two independent point-in-time reports (runs are not deduplicated).
	//
	// Verification is resource-heavy, so the stack caps the number of runs that may
	// execute **concurrently** (configured; default 1). When that cap is already
	// reached the call returns `429` — the server never stops a run to make room. To
	// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
	// what is running and free a slot deliberately: `cancel` a run to stop it while
	// keeping its report, or `DELETE` it to remove it entirely.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /system/verifications (the `StartVerification` operationId).
	StartVerification(ctx context.Context, body StartVerificationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteVerification Delete a verification run
	//
	// Really deletes the run and its report. If the run is still `running` it is
	// stopped first, then the record is removed — so this both aborts a run and
	// cleans up finished history in one step. Unlike the cancel action, nothing
	// survives: a subsequent `GET` is `404`. Either verb frees a concurrency slot;
	// cancel keeps the report, delete removes it.
	//
	// Corresponds with DELETE /system/verifications/{reportId} (the `DeleteVerification` operationId).
	DeleteVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetVerification Show a verification run
	//
	// The report for a run. Poll until `status` leaves `running`; while it is still
	// running the response carries a `Retry-After` hint and the progress counters
	// (`claimsChecked`, `bytesRead`) advance between polls.
	//
	// Corresponds with GET /system/verifications/{reportId} (the `GetVerification` operationId).
	GetVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CancelVerification Cancel a running verification run
	//
	// Stops a `running` run but **keeps** its report: the record stays in history
	// with `status` `stopped` and whatever partial findings it had gathered, and the
	// concurrency slot is freed. Use this to abort a run you want to keep a record
	// of; use `DELETE` to remove it entirely. Idempotent — cancelling a run that has
	// already finished or stopped returns its current report unchanged.
	//
	// Corresponds with POST /system/verifications/{reportId}/cancel (the `CancelVerification` operationId).
	CancelVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Whoami What this credential may do
	//
	// Reports the caller back to itself: the account its credential resolved to,
	// that account's grants, and any caveats attenuating them. It answers about
	// **this server's** access policy, never about the graph, which is why it sits
	// under `/system`.
	//
	// Needs no grant — a caller learns only what it already proved by
	// authenticating, and nothing about any other account. A bad credential is
	// still `401`, so this doubles as the side-effect-free way to check one.
	//
	// `grants` and `caveats` are reported separately rather than intersected: a
	// request is allowed when the account's grants permit it **and** every caveat
	// still does, so seeing both is what explains a refusal. A token narrowed by
	// attenuation has no other way to show what survived.
	//
	// Corresponds with GET /system/whoami (the `Whoami` operationId).
	Whoami(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetClaim Fetch a claim by id from the Universe (privileged)
	//
	// Returns claim `{id}` as its signed CBOR bytes from the Universe under **no
	// closure at all** — no branch table, and no confinement to the current head.
	// This is what makes it privileged, and what it exists for: reaching an archive
	// from a Universe and a head id alone, as when restoring from a head kept outside
	// the server. A claim the Universe holds is returned even where
	// `GET /archive/claims/{id}` reports it not-found.
	//
	// This collection reads the `$universe` scope: the same scope a RankeQL body names
	// as `select.branch: "$universe"`, and the same target a grant is written against.
	// It requires the **R** right on `$universe`, to which only **R** applies. An
	// ordinary glob confers it by no accident — a `$`-prefixed target needs an exact
	// grant, so `R *` reaches neither reserved scope.
	//
	// Immutably cacheable by id.
	//
	// Corresponds with GET /universe/claims/{id} (the `GetClaim` operationId).
	GetClaim(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetClaimContent Fetch the content of a claim by id from the Universe (privileged)
	//
	// Streams the content of claim `{id}` from the Universe under no closure — the
	// privileged read, conferred only through **R** on `$universe`. Content is
	// addressed by the claim; whether the bytes are inline or a separate blob is
	// hidden. Immutably cacheable by the claim id.
	//
	// Corresponds with GET /universe/claims/{id}/content (the `GetClaimContent` operationId).
	GetClaimContent(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// GetArchiveClaim Fetch a claim within the archive head's closure
//
// Returns claim `{id}` as its signed CBOR bytes if it lies in the closure of the
// **whole Ranke-Archive** — the current branch-table head — whichever branch holds
// it. A client reaches a claim without naming the branch it is on; a claim outside
// that closure returns `404`.
//
// This collection reads the `$archive` scope: the same scope a RankeQL body names
// as `select.branch: "$archive"`, and the same target a grant is written against.
// It requires the **R** right on `$archive`.
//
// Immutably cacheable by id.
//
// Corresponds with GET /archive/claims/{id} (the `GetArchiveClaim` operationId).
func (c *Client) GetArchiveClaim(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetArchiveClaimRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetArchiveClaimContent Fetch the content of a claim within the archive head's closure
//
// Streams the content of claim `{id}` if it lies in the archive head's closure,
// across every branch (same closure guarantee as the claim itself; outside it or
// unknown → `404`). Reads the `$archive` scope and requires **R** on `$archive`.
// Content is addressed by the claim; whether the bytes are inline or a separate
// blob is hidden. Immutably cacheable by the claim id.
//
// Corresponds with GET /archive/claims/{id}/content (the `GetArchiveClaimContent` operationId).
func (c *Client) GetArchiveClaimContent(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetArchiveClaimContentRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetArchiveInfo What is known about the archive
//
// Reports the Ranke-Archive as a whole: the **branch-table head** — the id every
// archive-scoped read is rooted at — with its height and when it last moved, and how
// many branches the table holds.
//
// This is the only route that reports the branch-table head, which is what a client
// needs to name the `$archive` scope in a query or a grant.
//
// Reads the `$archive` scope and requires the **R** right on `$archive`. Cacheable
// **with revalidation**: the head advances on every contribution.
//
// Corresponds with GET /archive/info (the `GetArchiveInfo` operationId).
func (c *Client) GetArchiveInfo(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetArchiveInfoRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBranches List the branch table's branches
//
// Returns every branch the branch table holds, each with its name and current
// head id. Reachable **without knowing any branch name**, so a client discovers
// what it may address before using the routes that take one; an archive with no
// branches yet answers with an empty list.
//
// Requires the **R** right on the reserved `$branches` target, which is what
// core-access grants as "enumerates the table".
//
// Cacheable **with revalidation** (weak `ETag`, `Cache-Control: no-cache`): every
// head in the list moves as its branch is contributed to. The listing is answered
// from **one archive snapshot**, so the heads are consistent with each other.
//
// Corresponds with GET /branches (the `ListBranches` operationId).
func (c *Client) ListBranches(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBranchesRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBranchClaim Fetch a claim within a branch's closure
//
// Returns claim `{id}` as its signed CBOR bytes, **only if it lies in branch
// `{branch}`'s closure**. A claim that is superseded, contradicted, or otherwise
// outside the closure returns `404`, indistinguishable from one that does not
// exist. Requires the **R** right on that branch.
//
// `{branch}` names an **ordinary branch**. A reserved scope name supplied here
// names a branch that does not exist and is answered as `404`; each scope has
// exactly one route (`/archive/…`, `/universe/…`).
//
// Immutably **cacheable** by id (strong `ETag`, `Cache-Control: public,
// immutable`): the id content-addresses the bytes, so they never change.
//
// Corresponds with GET /branches/{branch}/claims/{id} (the `GetBranchClaim` operationId).
func (c *Client) GetBranchClaim(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBranchClaimRequest(c.Server, branch, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBranchClaimContent Fetch the content of a claim within a branch's closure
//
// Streams the content of claim `{id}` — **only if it lies in branch `{branch}`'s
// closure** (same closure guarantee as the claim itself; out-of-closure or
// unknown → `404`). Requires the **R** right on that branch. Content is addressed
// by the claim that holds it, not by a raw hash: the server resolves whether the
// bytes live inline in the claim or in a separate blob, so the client can't tell
// and doesn't need to — and the read stays scoped as the route is scoped.
// Immutably **cacheable** by the claim id.
//
// Corresponds with GET /branches/{branch}/claims/{id}/content (the `GetBranchClaimContent` operationId).
func (c *Client) GetBranchClaimContent(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBranchClaimContentRequest(c.Server, branch, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBranchHead Current head id of a branch
//
// Returns the branch's current head id — a moving target (it advances on every
// contribution). Requires the **R** right on that branch. Cacheable **with
// revalidation** (weak `ETag`, `Cache-Control: no-cache`): a conditional request
// is cheap when the head has not moved. To inspect the head claim itself, fetch it
// via `GET /branches/{branch}/claims/{id}`.
//
// Corresponds with GET /branches/{branch}/head (the `GetBranchHead` operationId).
func (c *Client) GetBranchHead(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBranchHeadRequest(c.Server, branch)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBranchInfo What is known about a branch
//
// Reports a branch beyond its head id: the head's **height** — the generation number
// of the branch's newest claim, so the depth of what it points at — and **when it last
// moved**, which is the head claim's `created_at`.
//
// Everything here comes from the head claim, so it costs one claim read. A claim count
// is deliberately absent: counting a branch's claims is a walk of its closure, which is
// a query (`POST /query`) and not a field.
//
// Requires the **R** right on the branch. Cacheable **with revalidation** — every
// field moves when the branch does.
//
// Corresponds with GET /branches/{branch}/info (the `GetBranchInfo` operationId).
func (c *Client) GetBranchInfo(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBranchInfoRequest(c.Server, branch)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ContributeWithBody Contribute signed claims (atomic)
//
// Contributes one or more signed claims to a branch as a single **atomic**
// merge — all are absorbed under one new branch-table head, or none are.
// Content-addressed and therefore **idempotent**: re-contributing yields the same
// ids with no duplicates.
//
// The body is ranke's contribution stream: a **CBOR sequence** (RFC 8742) of
// records, self-framing so a contribution of any size streams.
//
//   - `[2, ["<branch>", …]]` — **first**, the branches this contribution touches
//   - `[0, <id>, <claim bytes>, "<branch>"]` — a claim, as the canonical CBOR its
//     contributor signed, and the branch it joins
//   - `[1, <hash>, <content bytes>]` — externalized content, which lives in the
//     Universe unbranched and so names no branch
//
// The header comes first so the **C** right is settled on every declared branch
// before any of the body is read; a claim naming an undeclared branch is refused, so
// the declaration binds. One contribution may therefore advance **several branches**,
// and an unauthorized one is answered without its payload being read.
//
// Payloads are CBOR byte strings carried through untouched: a claim's id is a
// signature over exactly those bytes, so the server stores what it was sent. The id
// is explicit because it cannot be derived — `Sign(H(S(node)))` is made with the
// contributor's key, and the canonical encoding carries only the node. Content is
// checked against the hash addressing it.
//
// Send every claim the closure needs that the archive may not hold yet — a
// first-time contributor includes its own `contribution/contributor` claim, since
// everything it signs references it.
//
// The sequencer mints the `contribution/branches` branch-table claim recording the
// merge (paper 02 §Sequencer). On success the new branch-table head id and the
// contributed claim ids are returned.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /contribute (the `Contribute` operationId).
func (c *Client) ContributeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewContributeRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AdvanceDevClockWithBody Advance the dev sequencer's clock
//
// Available **only** when the stack was launched with `--dev` against a `dev`
// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
// timestamps from forward to (at least) the given instant, so a client that
// knows its own story's schedule — a fixture generator, say — can make the
// archive's *recorded* history track its *narrated* one, one contribution at a
// time, rather than every merge landing at the real wall-clock moment the
// client happened to run.
//
// The clock never moves backward: a request older than its current position
// is accepted and answered with the position unchanged. Absent `--dev`, or
// against a `concurrent` (production) Sequencer, the route is `501` — the
// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
func (c *Client) AdvanceDevClockWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAdvanceDevClockRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AdvanceDevClock Advance the dev sequencer's clock
//
// Available **only** when the stack was launched with `--dev` against a `dev`
// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
// timestamps from forward to (at least) the given instant, so a client that
// knows its own story's schedule — a fixture generator, say — can make the
// archive's *recorded* history track its *narrated* one, one contribution at a
// time, rather than every merge landing at the real wall-clock moment the
// client happened to run.
//
// The clock never moves backward: a request older than its current position
// is accepted and answered with the position unchanged. Absent `--dev`, or
// against a `concurrent` (production) Sequencer, the route is `501` — the
// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
func (c *Client) AdvanceDevClock(ctx context.Context, body AdvanceDevClockJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAdvanceDevClockRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Health Liveness and signer identity
//
// Reports that the stack is up and the signing/contributor identity it
// attests merges under. Whether this requires a privileged subject is a
// core-access decision, not part of this contract.
//
// Corresponds with GET /health (the `Health` operationId).
func (c *Client) Health(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// QueryWithBody Read the graph with a declarative query
//
// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
// result, in the serialization chosen by `output.encoding`. Results carry the
// natural `(created_at, id)` order unless `order` sorts them, with its keys
// applied in priority order and that natural order breaking any remaining ties;
// to page, carry the last result's order key into the next request's `where`
// (pin `select.head` so the closure cannot shift between pages).
//
// When `execution.report` names a verbosity, the **final item** in the sequence
// is a `QueryReport` (see the schema) — typed distinctly from result items and
// always last — carrying the execution log, timing, and whether a limit truncated
// the read. `execution.layer` pins which storage layer runs it.
//
// The response media type mirrors `output.encoding`: `application/json-seq` or
// `application/cbor-seq`. A branch named in `select` that does not exist is
// `404`; claims outside the scope's closure simply do not appear. `$universe`
// is the privileged unconfined read and requires `select.head`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /query (the `Query` operationId).
func (c *Client) QueryWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewQueryRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Query Read the graph with a declarative query
//
// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
// result, in the serialization chosen by `output.encoding`. Results carry the
// natural `(created_at, id)` order unless `order` sorts them, with its keys
// applied in priority order and that natural order breaking any remaining ties;
// to page, carry the last result's order key into the next request's `where`
// (pin `select.head` so the closure cannot shift between pages).
//
// When `execution.report` names a verbosity, the **final item** in the sequence
// is a `QueryReport` (see the schema) — typed distinctly from result items and
// always last — carrying the execution log, timing, and whether a limit truncated
// the read. `execution.layer` pins which storage layer runs it.
//
// The response media type mirrors `output.encoding`: `application/json-seq` or
// `application/cbor-seq`. A branch named in `select` that does not exist is
// `404`; claims outside the scope's closure simply do not appear. `$universe`
// is the privileged unconfined read and requires `select.head`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /query (the `Query` operationId).
func (c *Client) Query(ctx context.Context, body QueryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewQueryRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListStorageLayers List storage layers
//
// Lists the stack's storage layers (read-through tiers) by **name and type
// only** — never connection details or secrets. Naming layers is what lets a
// verification run target one directly (a read-through view can mask the loss
// of an object on a deeper layer).
//
// Corresponds with GET /system/layers (the `ListStorageLayers` operationId).
func (c *Client) ListStorageLayers(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListStorageLayersRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListVerifications List verification runs
//
// Verification runs across the whole stack, newest first. Each report is a
// **point-in-time record**: a layer repaired externally shows clean in a later
// run, so reports accumulate rather than overwrite — until explicitly removed
// with `DELETE /system/verifications/{reportId}`.
//
// The API lives under `/system/` because a verification run is a ranke-db
// extension beyond the paper's surface rather than archive content: it is a
// stack-wide operational resource, rooted at a closure named in the request body
// and not in the path. (Branch reads no longer occupy the root, so nothing here
// is avoiding a collision.)
//
// Corresponds with GET /system/verifications (the `ListVerifications` operationId).
func (c *Client) ListVerifications(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListVerificationsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// StartVerificationWithBody Start a verification run
//
// Starts a run from a `VerificationConfig`: walk the closure rooted at
// `closure` (a branch name — resolved to its current head and **pinned** for
// the life of the run — or a head id directly), reading the named `layer`
// directly, re-checking each claim to the configured depth. It returns
// *findings*, not contents, so it never leaks across closures.
//
// A run may take a **very long time** (hours to days over a large closure at
// full-content depth), so it is always **asynchronous**: the call returns `202`
// immediately with the running report and a `Location` header pointing at the
// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
// yields two independent point-in-time reports (runs are not deduplicated).
//
// Verification is resource-heavy, so the stack caps the number of runs that may
// execute **concurrently** (configured; default 1). When that cap is already
// reached the call returns `429` — the server never stops a run to make room. To
// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
// what is running and free a slot deliberately: `cancel` a run to stop it while
// keeping its report, or `DELETE` it to remove it entirely.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /system/verifications (the `StartVerification` operationId).
func (c *Client) StartVerificationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewStartVerificationRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// StartVerification Start a verification run
//
// Starts a run from a `VerificationConfig`: walk the closure rooted at
// `closure` (a branch name — resolved to its current head and **pinned** for
// the life of the run — or a head id directly), reading the named `layer`
// directly, re-checking each claim to the configured depth. It returns
// *findings*, not contents, so it never leaks across closures.
//
// A run may take a **very long time** (hours to days over a large closure at
// full-content depth), so it is always **asynchronous**: the call returns `202`
// immediately with the running report and a `Location` header pointing at the
// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
// yields two independent point-in-time reports (runs are not deduplicated).
//
// Verification is resource-heavy, so the stack caps the number of runs that may
// execute **concurrently** (configured; default 1). When that cap is already
// reached the call returns `429` — the server never stops a run to make room. To
// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
// what is running and free a slot deliberately: `cancel` a run to stop it while
// keeping its report, or `DELETE` it to remove it entirely.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /system/verifications (the `StartVerification` operationId).
func (c *Client) StartVerification(ctx context.Context, body StartVerificationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewStartVerificationRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteVerification Delete a verification run
//
// Really deletes the run and its report. If the run is still `running` it is
// stopped first, then the record is removed — so this both aborts a run and
// cleans up finished history in one step. Unlike the cancel action, nothing
// survives: a subsequent `GET` is `404`. Either verb frees a concurrency slot;
// cancel keeps the report, delete removes it.
//
// Corresponds with DELETE /system/verifications/{reportId} (the `DeleteVerification` operationId).
func (c *Client) DeleteVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteVerificationRequest(c.Server, reportId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetVerification Show a verification run
//
// The report for a run. Poll until `status` leaves `running`; while it is still
// running the response carries a `Retry-After` hint and the progress counters
// (`claimsChecked`, `bytesRead`) advance between polls.
//
// Corresponds with GET /system/verifications/{reportId} (the `GetVerification` operationId).
func (c *Client) GetVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetVerificationRequest(c.Server, reportId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CancelVerification Cancel a running verification run
//
// Stops a `running` run but **keeps** its report: the record stays in history
// with `status` `stopped` and whatever partial findings it had gathered, and the
// concurrency slot is freed. Use this to abort a run you want to keep a record
// of; use `DELETE` to remove it entirely. Idempotent — cancelling a run that has
// already finished or stopped returns its current report unchanged.
//
// Corresponds with POST /system/verifications/{reportId}/cancel (the `CancelVerification` operationId).
func (c *Client) CancelVerification(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCancelVerificationRequest(c.Server, reportId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Whoami What this credential may do
//
// Reports the caller back to itself: the account its credential resolved to,
// that account's grants, and any caveats attenuating them. It answers about
// **this server's** access policy, never about the graph, which is why it sits
// under `/system`.
//
// Needs no grant — a caller learns only what it already proved by
// authenticating, and nothing about any other account. A bad credential is
// still `401`, so this doubles as the side-effect-free way to check one.
//
// `grants` and `caveats` are reported separately rather than intersected: a
// request is allowed when the account's grants permit it **and** every caveat
// still does, so seeing both is what explains a refusal. A token narrowed by
// attenuation has no other way to show what survived.
//
// Corresponds with GET /system/whoami (the `Whoami` operationId).
func (c *Client) Whoami(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewWhoamiRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetClaim Fetch a claim by id from the Universe (privileged)
//
// Returns claim `{id}` as its signed CBOR bytes from the Universe under **no
// closure at all** — no branch table, and no confinement to the current head.
// This is what makes it privileged, and what it exists for: reaching an archive
// from a Universe and a head id alone, as when restoring from a head kept outside
// the server. A claim the Universe holds is returned even where
// `GET /archive/claims/{id}` reports it not-found.
//
// This collection reads the `$universe` scope: the same scope a RankeQL body names
// as `select.branch: "$universe"`, and the same target a grant is written against.
// It requires the **R** right on `$universe`, to which only **R** applies. An
// ordinary glob confers it by no accident — a `$`-prefixed target needs an exact
// grant, so `R *` reaches neither reserved scope.
//
// Immutably cacheable by id.
//
// Corresponds with GET /universe/claims/{id} (the `GetClaim` operationId).
func (c *Client) GetClaim(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetClaimRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetClaimContent Fetch the content of a claim by id from the Universe (privileged)
//
// Streams the content of claim `{id}` from the Universe under no closure — the
// privileged read, conferred only through **R** on `$universe`. Content is
// addressed by the claim; whether the bytes are inline or a separate blob is
// hidden. Immutably cacheable by the claim id.
//
// Corresponds with GET /universe/claims/{id}/content (the `GetClaimContent` operationId).
func (c *Client) GetClaimContent(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetClaimContentRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewGetArchiveClaimRequest constructs an http.Request for the GetArchiveClaim method
func NewGetArchiveClaimRequest(server string, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/archive/claims/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetArchiveClaimContentRequest constructs an http.Request for the GetArchiveClaimContent method
func NewGetArchiveClaimContentRequest(server string, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/archive/claims/%s/content", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetArchiveInfoRequest constructs an http.Request for the GetArchiveInfo method
func NewGetArchiveInfoRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/archive/info")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListBranchesRequest constructs an http.Request for the ListBranches method
func NewListBranchesRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/branches")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBranchClaimRequest constructs an http.Request for the GetBranchClaim method
func NewGetBranchClaimRequest(server string, branch BranchName, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "branch", branch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/branches/%s/claims/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBranchClaimContentRequest constructs an http.Request for the GetBranchClaimContent method
func NewGetBranchClaimContentRequest(server string, branch BranchName, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "branch", branch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/branches/%s/claims/%s/content", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBranchHeadRequest constructs an http.Request for the GetBranchHead method
func NewGetBranchHeadRequest(server string, branch BranchName) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "branch", branch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/branches/%s/head", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBranchInfoRequest constructs an http.Request for the GetBranchInfo method
func NewGetBranchInfoRequest(server string, branch BranchName) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "branch", branch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/branches/%s/info", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewContributeRequestWithBody constructs an http.Request for the Contribute method, with any body, and a specified content type
func NewContributeRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/contribute")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewAdvanceDevClockRequest calls the generic AdvanceDevClock builder with application/json body
func NewAdvanceDevClockRequest(server string, body AdvanceDevClockJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAdvanceDevClockRequestWithBody(server, "application/json", bodyReader)
}

// NewAdvanceDevClockRequestWithBody constructs an http.Request for the AdvanceDevClock method, with any body, and a specified content type
func NewAdvanceDevClockRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/dev/clock")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewHealthRequest constructs an http.Request for the Health method
func NewHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/health")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewQueryRequest calls the generic Query builder with application/json body
func NewQueryRequest(server string, body QueryJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewQueryRequestWithBody(server, "application/json", bodyReader)
}

// NewQueryRequestWithBody constructs an http.Request for the Query method, with any body, and a specified content type
func NewQueryRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/query")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListStorageLayersRequest constructs an http.Request for the ListStorageLayers method
func NewListStorageLayersRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/layers")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListVerificationsRequest constructs an http.Request for the ListVerifications method
func NewListVerificationsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/verifications")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewStartVerificationRequest calls the generic StartVerification builder with application/json body
func NewStartVerificationRequest(server string, body StartVerificationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewStartVerificationRequestWithBody(server, "application/json", bodyReader)
}

// NewStartVerificationRequestWithBody constructs an http.Request for the StartVerification method, with any body, and a specified content type
func NewStartVerificationRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/verifications")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteVerificationRequest constructs an http.Request for the DeleteVerification method
func NewDeleteVerificationRequest(server string, reportId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "reportId", reportId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/verifications/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetVerificationRequest constructs an http.Request for the GetVerification method
func NewGetVerificationRequest(server string, reportId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "reportId", reportId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/verifications/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCancelVerificationRequest constructs an http.Request for the CancelVerification method
func NewCancelVerificationRequest(server string, reportId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "reportId", reportId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/verifications/%s/cancel", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewWhoamiRequest constructs an http.Request for the Whoami method
func NewWhoamiRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/system/whoami")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetClaimRequest constructs an http.Request for the GetClaim method
func NewGetClaimRequest(server string, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/universe/claims/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetClaimContentRequest constructs an http.Request for the GetClaimContent method
func NewGetClaimContentRequest(server string, id ClaimId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/universe/claims/%s/content", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// GetArchiveClaimWithResponse Fetch a claim within the archive head's closure
	//
	// Returns claim `{id}` as its signed CBOR bytes if it lies in the closure of the
	// **whole Ranke-Archive** — the current branch-table head — whichever branch holds
	// it. A client reaches a claim without naming the branch it is on; a claim outside
	// that closure returns `404`.
	//
	// This collection reads the `$archive` scope: the same scope a RankeQL body names
	// as `select.branch: "$archive"`, and the same target a grant is written against.
	// It requires the **R** right on `$archive`.
	//
	// Immutably cacheable by id.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /archive/claims/{id} (the `GetArchiveClaim` operationId).
	GetArchiveClaimWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetArchiveClaimResponse, error)

	// GetArchiveClaimContentWithResponse Fetch the content of a claim within the archive head's closure
	//
	// Streams the content of claim `{id}` if it lies in the archive head's closure,
	// across every branch (same closure guarantee as the claim itself; outside it or
	// unknown → `404`). Reads the `$archive` scope and requires **R** on `$archive`.
	// Content is addressed by the claim; whether the bytes are inline or a separate
	// blob is hidden. Immutably cacheable by the claim id.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /archive/claims/{id}/content (the `GetArchiveClaimContent` operationId).
	GetArchiveClaimContentWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetArchiveClaimContentResponse, error)

	// GetArchiveInfoWithResponse What is known about the archive
	//
	// Reports the Ranke-Archive as a whole: the **branch-table head** — the id every
	// archive-scoped read is rooted at — with its height and when it last moved, and how
	// many branches the table holds.
	//
	// This is the only route that reports the branch-table head, which is what a client
	// needs to name the `$archive` scope in a query or a grant.
	//
	// Reads the `$archive` scope and requires the **R** right on `$archive`. Cacheable
	// **with revalidation**: the head advances on every contribution.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /archive/info (the `GetArchiveInfo` operationId).
	GetArchiveInfoWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetArchiveInfoResponse, error)

	// ListBranchesWithResponse List the branch table's branches
	//
	// Returns every branch the branch table holds, each with its name and current
	// head id. Reachable **without knowing any branch name**, so a client discovers
	// what it may address before using the routes that take one; an archive with no
	// branches yet answers with an empty list.
	//
	// Requires the **R** right on the reserved `$branches` target, which is what
	// core-access grants as "enumerates the table".
	//
	// Cacheable **with revalidation** (weak `ETag`, `Cache-Control: no-cache`): every
	// head in the list moves as its branch is contributed to. The listing is answered
	// from **one archive snapshot**, so the heads are consistent with each other.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /branches (the `ListBranches` operationId).
	ListBranchesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBranchesResponse, error)

	// GetBranchClaimWithResponse Fetch a claim within a branch's closure
	//
	// Returns claim `{id}` as its signed CBOR bytes, **only if it lies in branch
	// `{branch}`'s closure**. A claim that is superseded, contradicted, or otherwise
	// outside the closure returns `404`, indistinguishable from one that does not
	// exist. Requires the **R** right on that branch.
	//
	// `{branch}` names an **ordinary branch**. A reserved scope name supplied here
	// names a branch that does not exist and is answered as `404`; each scope has
	// exactly one route (`/archive/…`, `/universe/…`).
	//
	// Immutably **cacheable** by id (strong `ETag`, `Cache-Control: public,
	// immutable`): the id content-addresses the bytes, so they never change.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /branches/{branch}/claims/{id} (the `GetBranchClaim` operationId).
	GetBranchClaimWithResponse(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*GetBranchClaimResponse, error)

	// GetBranchClaimContentWithResponse Fetch the content of a claim within a branch's closure
	//
	// Streams the content of claim `{id}` — **only if it lies in branch `{branch}`'s
	// closure** (same closure guarantee as the claim itself; out-of-closure or
	// unknown → `404`). Requires the **R** right on that branch. Content is addressed
	// by the claim that holds it, not by a raw hash: the server resolves whether the
	// bytes live inline in the claim or in a separate blob, so the client can't tell
	// and doesn't need to — and the read stays scoped as the route is scoped.
	// Immutably **cacheable** by the claim id.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /branches/{branch}/claims/{id}/content (the `GetBranchClaimContent` operationId).
	GetBranchClaimContentWithResponse(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*GetBranchClaimContentResponse, error)

	// GetBranchHeadWithResponse Current head id of a branch
	//
	// Returns the branch's current head id — a moving target (it advances on every
	// contribution). Requires the **R** right on that branch. Cacheable **with
	// revalidation** (weak `ETag`, `Cache-Control: no-cache`): a conditional request
	// is cheap when the head has not moved. To inspect the head claim itself, fetch it
	// via `GET /branches/{branch}/claims/{id}`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /branches/{branch}/head (the `GetBranchHead` operationId).
	GetBranchHeadWithResponse(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*GetBranchHeadResponse, error)

	// GetBranchInfoWithResponse What is known about a branch
	//
	// Reports a branch beyond its head id: the head's **height** — the generation number
	// of the branch's newest claim, so the depth of what it points at — and **when it last
	// moved**, which is the head claim's `created_at`.
	//
	// Everything here comes from the head claim, so it costs one claim read. A claim count
	// is deliberately absent: counting a branch's claims is a walk of its closure, which is
	// a query (`POST /query`) and not a field.
	//
	// Requires the **R** right on the branch. Cacheable **with revalidation** — every
	// field moves when the branch does.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /branches/{branch}/info (the `GetBranchInfo` operationId).
	GetBranchInfoWithResponse(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*GetBranchInfoResponse, error)

	// ContributeWithBodyWithResponse Contribute signed claims (atomic)
	//
	// Contributes one or more signed claims to a branch as a single **atomic**
	// merge — all are absorbed under one new branch-table head, or none are.
	// Content-addressed and therefore **idempotent**: re-contributing yields the same
	// ids with no duplicates.
	//
	// The body is ranke's contribution stream: a **CBOR sequence** (RFC 8742) of
	// records, self-framing so a contribution of any size streams.
	//
	//   - `[2, ["<branch>", …]]` — **first**, the branches this contribution touches
	//   - `[0, <id>, <claim bytes>, "<branch>"]` — a claim, as the canonical CBOR its
	//     contributor signed, and the branch it joins
	//   - `[1, <hash>, <content bytes>]` — externalized content, which lives in the
	//     Universe unbranched and so names no branch
	//
	// The header comes first so the **C** right is settled on every declared branch
	// before any of the body is read; a claim naming an undeclared branch is refused, so
	// the declaration binds. One contribution may therefore advance **several branches**,
	// and an unauthorized one is answered without its payload being read.
	//
	// Payloads are CBOR byte strings carried through untouched: a claim's id is a
	// signature over exactly those bytes, so the server stores what it was sent. The id
	// is explicit because it cannot be derived — `Sign(H(S(node)))` is made with the
	// contributor's key, and the canonical encoding carries only the node. Content is
	// checked against the hash addressing it.
	//
	// Send every claim the closure needs that the archive may not hold yet — a
	// first-time contributor includes its own `contribution/contributor` claim, since
	// everything it signs references it.
	//
	// The sequencer mints the `contribution/branches` branch-table claim recording the
	// merge (paper 02 §Sequencer). On success the new branch-table head id and the
	// contributed claim ids are returned.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /contribute (the `Contribute` operationId).
	ContributeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ContributeResponse, error)

	// AdvanceDevClockWithBodyWithResponse Advance the dev sequencer's clock
	//
	// Available **only** when the stack was launched with `--dev` against a `dev`
	// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
	// timestamps from forward to (at least) the given instant, so a client that
	// knows its own story's schedule — a fixture generator, say — can make the
	// archive's *recorded* history track its *narrated* one, one contribution at a
	// time, rather than every merge landing at the real wall-clock moment the
	// client happened to run.
	//
	// The clock never moves backward: a request older than its current position
	// is accepted and answered with the position unchanged. Absent `--dev`, or
	// against a `concurrent` (production) Sequencer, the route is `501` — the
	// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
	AdvanceDevClockWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AdvanceDevClockResponse, error)

	// AdvanceDevClockWithResponse Advance the dev sequencer's clock
	//
	// Available **only** when the stack was launched with `--dev` against a `dev`
	// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
	// timestamps from forward to (at least) the given instant, so a client that
	// knows its own story's schedule — a fixture generator, say — can make the
	// archive's *recorded* history track its *narrated* one, one contribution at a
	// time, rather than every merge landing at the real wall-clock moment the
	// client happened to run.
	//
	// The clock never moves backward: a request older than its current position
	// is accepted and answered with the position unchanged. Absent `--dev`, or
	// against a `concurrent` (production) Sequencer, the route is `501` — the
	// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
	AdvanceDevClockWithResponse(ctx context.Context, body AdvanceDevClockJSONRequestBody, reqEditors ...RequestEditorFn) (*AdvanceDevClockResponse, error)

	// HealthWithResponse Liveness and signer identity
	//
	// Reports that the stack is up and the signing/contributor identity it
	// attests merges under. Whether this requires a privileged subject is a
	// core-access decision, not part of this contract.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /health (the `Health` operationId).
	HealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*HealthResponse, error)

	// QueryWithBodyWithResponse Read the graph with a declarative query
	//
	// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
	// result, in the serialization chosen by `output.encoding`. Results carry the
	// natural `(created_at, id)` order unless `order` sorts them, with its keys
	// applied in priority order and that natural order breaking any remaining ties;
	// to page, carry the last result's order key into the next request's `where`
	// (pin `select.head` so the closure cannot shift between pages).
	//
	// When `execution.report` names a verbosity, the **final item** in the sequence
	// is a `QueryReport` (see the schema) — typed distinctly from result items and
	// always last — carrying the execution log, timing, and whether a limit truncated
	// the read. `execution.layer` pins which storage layer runs it.
	//
	// The response media type mirrors `output.encoding`: `application/json-seq` or
	// `application/cbor-seq`. A branch named in `select` that does not exist is
	// `404`; claims outside the scope's closure simply do not appear. `$universe`
	// is the privileged unconfined read and requires `select.head`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /query (the `Query` operationId).
	QueryWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*QueryResponse, error)

	// QueryWithResponse Read the graph with a declarative query
	//
	// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
	// result, in the serialization chosen by `output.encoding`. Results carry the
	// natural `(created_at, id)` order unless `order` sorts them, with its keys
	// applied in priority order and that natural order breaking any remaining ties;
	// to page, carry the last result's order key into the next request's `where`
	// (pin `select.head` so the closure cannot shift between pages).
	//
	// When `execution.report` names a verbosity, the **final item** in the sequence
	// is a `QueryReport` (see the schema) — typed distinctly from result items and
	// always last — carrying the execution log, timing, and whether a limit truncated
	// the read. `execution.layer` pins which storage layer runs it.
	//
	// The response media type mirrors `output.encoding`: `application/json-seq` or
	// `application/cbor-seq`. A branch named in `select` that does not exist is
	// `404`; claims outside the scope's closure simply do not appear. `$universe`
	// is the privileged unconfined read and requires `select.head`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /query (the `Query` operationId).
	QueryWithResponse(ctx context.Context, body QueryJSONRequestBody, reqEditors ...RequestEditorFn) (*QueryResponse, error)

	// ListStorageLayersWithResponse List storage layers
	//
	// Lists the stack's storage layers (read-through tiers) by **name and type
	// only** — never connection details or secrets. Naming layers is what lets a
	// verification run target one directly (a read-through view can mask the loss
	// of an object on a deeper layer).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /system/layers (the `ListStorageLayers` operationId).
	ListStorageLayersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListStorageLayersResponse, error)

	// ListVerificationsWithResponse List verification runs
	//
	// Verification runs across the whole stack, newest first. Each report is a
	// **point-in-time record**: a layer repaired externally shows clean in a later
	// run, so reports accumulate rather than overwrite — until explicitly removed
	// with `DELETE /system/verifications/{reportId}`.
	//
	// The API lives under `/system/` because a verification run is a ranke-db
	// extension beyond the paper's surface rather than archive content: it is a
	// stack-wide operational resource, rooted at a closure named in the request body
	// and not in the path. (Branch reads no longer occupy the root, so nothing here
	// is avoiding a collision.)
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /system/verifications (the `ListVerifications` operationId).
	ListVerificationsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListVerificationsResponse, error)

	// StartVerificationWithBodyWithResponse Start a verification run
	//
	// Starts a run from a `VerificationConfig`: walk the closure rooted at
	// `closure` (a branch name — resolved to its current head and **pinned** for
	// the life of the run — or a head id directly), reading the named `layer`
	// directly, re-checking each claim to the configured depth. It returns
	// *findings*, not contents, so it never leaks across closures.
	//
	// A run may take a **very long time** (hours to days over a large closure at
	// full-content depth), so it is always **asynchronous**: the call returns `202`
	// immediately with the running report and a `Location` header pointing at the
	// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
	// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
	// yields two independent point-in-time reports (runs are not deduplicated).
	//
	// Verification is resource-heavy, so the stack caps the number of runs that may
	// execute **concurrently** (configured; default 1). When that cap is already
	// reached the call returns `429` — the server never stops a run to make room. To
	// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
	// what is running and free a slot deliberately: `cancel` a run to stop it while
	// keeping its report, or `DELETE` it to remove it entirely.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /system/verifications (the `StartVerification` operationId).
	StartVerificationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*StartVerificationResponse, error)

	// StartVerificationWithResponse Start a verification run
	//
	// Starts a run from a `VerificationConfig`: walk the closure rooted at
	// `closure` (a branch name — resolved to its current head and **pinned** for
	// the life of the run — or a head id directly), reading the named `layer`
	// directly, re-checking each claim to the configured depth. It returns
	// *findings*, not contents, so it never leaks across closures.
	//
	// A run may take a **very long time** (hours to days over a large closure at
	// full-content depth), so it is always **asynchronous**: the call returns `202`
	// immediately with the running report and a `Location` header pointing at the
	// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
	// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
	// yields two independent point-in-time reports (runs are not deduplicated).
	//
	// Verification is resource-heavy, so the stack caps the number of runs that may
	// execute **concurrently** (configured; default 1). When that cap is already
	// reached the call returns `429` — the server never stops a run to make room. To
	// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
	// what is running and free a slot deliberately: `cancel` a run to stop it while
	// keeping its report, or `DELETE` it to remove it entirely.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /system/verifications (the `StartVerification` operationId).
	StartVerificationWithResponse(ctx context.Context, body StartVerificationJSONRequestBody, reqEditors ...RequestEditorFn) (*StartVerificationResponse, error)

	// DeleteVerificationWithResponse Delete a verification run
	//
	// Really deletes the run and its report. If the run is still `running` it is
	// stopped first, then the record is removed — so this both aborts a run and
	// cleans up finished history in one step. Unlike the cancel action, nothing
	// survives: a subsequent `GET` is `404`. Either verb frees a concurrency slot;
	// cancel keeps the report, delete removes it.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /system/verifications/{reportId} (the `DeleteVerification` operationId).
	DeleteVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*DeleteVerificationResponse, error)

	// GetVerificationWithResponse Show a verification run
	//
	// The report for a run. Poll until `status` leaves `running`; while it is still
	// running the response carries a `Retry-After` hint and the progress counters
	// (`claimsChecked`, `bytesRead`) advance between polls.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /system/verifications/{reportId} (the `GetVerification` operationId).
	GetVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*GetVerificationResponse, error)

	// CancelVerificationWithResponse Cancel a running verification run
	//
	// Stops a `running` run but **keeps** its report: the record stays in history
	// with `status` `stopped` and whatever partial findings it had gathered, and the
	// concurrency slot is freed. Use this to abort a run you want to keep a record
	// of; use `DELETE` to remove it entirely. Idempotent — cancelling a run that has
	// already finished or stopped returns its current report unchanged.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /system/verifications/{reportId}/cancel (the `CancelVerification` operationId).
	CancelVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*CancelVerificationResponse, error)

	// WhoamiWithResponse What this credential may do
	//
	// Reports the caller back to itself: the account its credential resolved to,
	// that account's grants, and any caveats attenuating them. It answers about
	// **this server's** access policy, never about the graph, which is why it sits
	// under `/system`.
	//
	// Needs no grant — a caller learns only what it already proved by
	// authenticating, and nothing about any other account. A bad credential is
	// still `401`, so this doubles as the side-effect-free way to check one.
	//
	// `grants` and `caveats` are reported separately rather than intersected: a
	// request is allowed when the account's grants permit it **and** every caveat
	// still does, so seeing both is what explains a refusal. A token narrowed by
	// attenuation has no other way to show what survived.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /system/whoami (the `Whoami` operationId).
	WhoamiWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*WhoamiResponse, error)

	// GetClaimWithResponse Fetch a claim by id from the Universe (privileged)
	//
	// Returns claim `{id}` as its signed CBOR bytes from the Universe under **no
	// closure at all** — no branch table, and no confinement to the current head.
	// This is what makes it privileged, and what it exists for: reaching an archive
	// from a Universe and a head id alone, as when restoring from a head kept outside
	// the server. A claim the Universe holds is returned even where
	// `GET /archive/claims/{id}` reports it not-found.
	//
	// This collection reads the `$universe` scope: the same scope a RankeQL body names
	// as `select.branch: "$universe"`, and the same target a grant is written against.
	// It requires the **R** right on `$universe`, to which only **R** applies. An
	// ordinary glob confers it by no accident — a `$`-prefixed target needs an exact
	// grant, so `R *` reaches neither reserved scope.
	//
	// Immutably cacheable by id.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /universe/claims/{id} (the `GetClaim` operationId).
	GetClaimWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetClaimResponse, error)

	// GetClaimContentWithResponse Fetch the content of a claim by id from the Universe (privileged)
	//
	// Streams the content of claim `{id}` from the Universe under no closure — the
	// privileged read, conferred only through **R** on `$universe`. Content is
	// addressed by the claim; whether the bytes are inline or a separate blob is
	// hidden. Immutably cacheable by the claim id.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /universe/claims/{id}/content (the `GetClaimContent` operationId).
	GetClaimContentWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetClaimContentResponse, error)
}

// GetArchiveClaimResponse200Headers the declared response headers of an HTTP 200 response for GetArchiveClaim
type GetArchiveClaimResponse200Headers struct {
	ETag *string
}

type GetArchiveClaimResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetArchiveClaimResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetArchiveClaimResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetArchiveClaimResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetArchiveClaimResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetArchiveClaimResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetArchiveClaimResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetArchiveClaimResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetArchiveClaimResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetArchiveClaimContentResponse200Headers the declared response headers of an HTTP 200 response for GetArchiveClaimContent
type GetArchiveClaimContentResponse200Headers struct {
	ETag *string
}

type GetArchiveClaimContentResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetArchiveClaimContentResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetArchiveClaimContentResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetArchiveClaimContentResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetArchiveClaimContentResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetArchiveClaimContentResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetArchiveClaimContentResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetArchiveClaimContentResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetArchiveClaimContentResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetArchiveInfoResponse200Headers the declared response headers of an HTTP 200 response for GetArchiveInfo
type GetArchiveInfoResponse200Headers struct {
	ETag *string
}

type GetArchiveInfoResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ArchiveInfo
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetArchiveInfoResponse200Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetArchiveInfoResponse) GetJSON200() *ArchiveInfo {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetArchiveInfoResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetArchiveInfoResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetArchiveInfoResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetArchiveInfoResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetArchiveInfoResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetArchiveInfoResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListBranchesResponse200Headers the declared response headers of an HTTP 200 response for ListBranches
type ListBranchesResponse200Headers struct {
	ETag *string
}

type ListBranchesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BranchList
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *ListBranchesResponse200Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListBranchesResponse) GetJSON200() *BranchList {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListBranchesResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBranchesResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListBranchesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBranchesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBranchesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBranchesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetBranchClaimResponse200Headers the declared response headers of an HTTP 200 response for GetBranchClaim
type GetBranchClaimResponse200Headers struct {
	ETag *string
}

type GetBranchClaimResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetBranchClaimResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBranchClaimResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBranchClaimResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBranchClaimResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetBranchClaimResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBranchClaimResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBranchClaimResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBranchClaimResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetBranchClaimContentResponse200Headers the declared response headers of an HTTP 200 response for GetBranchClaimContent
type GetBranchClaimContentResponse200Headers struct {
	ETag *string
}

type GetBranchClaimContentResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetBranchClaimContentResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBranchClaimContentResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBranchClaimContentResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBranchClaimContentResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetBranchClaimContentResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBranchClaimContentResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBranchClaimContentResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBranchClaimContentResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetBranchHeadResponse200Headers the declared response headers of an HTTP 200 response for GetBranchHead
type GetBranchHeadResponse200Headers struct {
	ETag *string
}

type GetBranchHeadResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BranchHead
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetBranchHeadResponse200Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBranchHeadResponse) GetJSON200() *BranchHead {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBranchHeadResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBranchHeadResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBranchHeadResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetBranchHeadResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBranchHeadResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBranchHeadResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBranchHeadResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetBranchInfoResponse200Headers the declared response headers of an HTTP 200 response for GetBranchInfo
type GetBranchInfoResponse200Headers struct {
	ETag *string
}

type GetBranchInfoResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BranchInfo
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetBranchInfoResponse200Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBranchInfoResponse) GetJSON200() *BranchInfo {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBranchInfoResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBranchInfoResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBranchInfoResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetBranchInfoResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBranchInfoResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBranchInfoResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBranchInfoResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ContributeResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ContributionResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r ContributeResponse) GetJSON201() *ContributionResult {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ContributeResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ContributeResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ContributeResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ContributeResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ContributeResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetBody returns the raw response body bytes
func (r ContributeResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ContributeResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ContributeResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ContributeResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AdvanceDevClockResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *DevClock
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON501 the response for an HTTP 501 `application/json` response
	JSON501 *NotImplemented
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r AdvanceDevClockResponse) GetJSON200() *DevClock {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r AdvanceDevClockResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r AdvanceDevClockResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r AdvanceDevClockResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON501 returns the response for an HTTP 501 `application/json` response
func (r AdvanceDevClockResponse) GetJSON501() *NotImplemented {
	return r.JSON501
}

// GetBody returns the raw response body bytes
func (r AdvanceDevClockResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AdvanceDevClockResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AdvanceDevClockResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AdvanceDevClockResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type HealthResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Health
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r HealthResponse) GetJSON200() *Health {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r HealthResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r HealthResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r HealthResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r HealthResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type QueryResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r QueryResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r QueryResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r QueryResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r QueryResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r QueryResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r QueryResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r QueryResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r QueryResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListStorageLayersResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *StorageLayerList
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListStorageLayersResponse) GetJSON200() *StorageLayerList {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListStorageLayersResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListStorageLayersResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListStorageLayersResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListStorageLayersResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListStorageLayersResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListStorageLayersResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListVerificationsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *VerificationReportList
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListVerificationsResponse) GetJSON200() *VerificationReportList {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListVerificationsResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListVerificationsResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListVerificationsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListVerificationsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListVerificationsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListVerificationsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// StartVerificationResponse202Headers the declared response headers of an HTTP 202 response for StartVerification
type StartVerificationResponse202Headers struct {
	Location   *string
	RetryAfter *int
}

// StartVerificationResponse429Headers the declared response headers of an HTTP 429 response for StartVerification
type StartVerificationResponse429Headers struct {
	RetryAfter *int
}

type StartVerificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *VerificationReport
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// Headers202 the parsed response headers for an HTTP 202 response
	Headers202 *StartVerificationResponse202Headers
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *StartVerificationResponse429Headers
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r StartVerificationResponse) GetJSON202() *VerificationReport {
	return r.JSON202
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r StartVerificationResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r StartVerificationResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r StartVerificationResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r StartVerificationResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetBody returns the raw response body bytes
func (r StartVerificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r StartVerificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r StartVerificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r StartVerificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteVerificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteVerificationResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteVerificationResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteVerificationResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r DeleteVerificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteVerificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteVerificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteVerificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetVerificationResponse200Headers the declared response headers of an HTTP 200 response for GetVerification
type GetVerificationResponse200Headers struct {
	RetryAfter *int
}

type GetVerificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *VerificationReport
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetVerificationResponse200Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetVerificationResponse) GetJSON200() *VerificationReport {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetVerificationResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetVerificationResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetVerificationResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetVerificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetVerificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetVerificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetVerificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CancelVerificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *VerificationReport
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CancelVerificationResponse) GetJSON200() *VerificationReport {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CancelVerificationResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CancelVerificationResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CancelVerificationResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r CancelVerificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CancelVerificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CancelVerificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CancelVerificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type WhoamiResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Subject
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r WhoamiResponse) GetJSON200() *Subject {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r WhoamiResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r WhoamiResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r WhoamiResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r WhoamiResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r WhoamiResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetClaimResponse200Headers the declared response headers of an HTTP 200 response for GetClaim
type GetClaimResponse200Headers struct {
	ETag *string
}

type GetClaimResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetClaimResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetClaimResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetClaimResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetClaimResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetClaimResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetClaimResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetClaimResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetClaimResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetClaimContentResponse200Headers the declared response headers of an HTTP 200 response for GetClaimContent
type GetClaimContentResponse200Headers struct {
	ETag *string
}

type GetClaimContentResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetClaimContentResponse200Headers
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetClaimContentResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetClaimContentResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetClaimContentResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetClaimContentResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetClaimContentResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetClaimContentResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetClaimContentResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetArchiveClaimWithResponse Fetch a claim within the archive head's closure
//
// Returns claim `{id}` as its signed CBOR bytes if it lies in the closure of the
// **whole Ranke-Archive** — the current branch-table head — whichever branch holds
// it. A client reaches a claim without naming the branch it is on; a claim outside
// that closure returns `404`.
//
// This collection reads the `$archive` scope: the same scope a RankeQL body names
// as `select.branch: "$archive"`, and the same target a grant is written against.
// It requires the **R** right on `$archive`.
//
// Immutably cacheable by id.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /archive/claims/{id} (the `GetArchiveClaim` operationId).
func (c *ClientWithResponses) GetArchiveClaimWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetArchiveClaimResponse, error) {
	rsp, err := c.GetArchiveClaim(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetArchiveClaimResponse(rsp)
}

// GetArchiveClaimContentWithResponse Fetch the content of a claim within the archive head's closure
//
// Streams the content of claim `{id}` if it lies in the archive head's closure,
// across every branch (same closure guarantee as the claim itself; outside it or
// unknown → `404`). Reads the `$archive` scope and requires **R** on `$archive`.
// Content is addressed by the claim; whether the bytes are inline or a separate
// blob is hidden. Immutably cacheable by the claim id.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /archive/claims/{id}/content (the `GetArchiveClaimContent` operationId).
func (c *ClientWithResponses) GetArchiveClaimContentWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetArchiveClaimContentResponse, error) {
	rsp, err := c.GetArchiveClaimContent(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetArchiveClaimContentResponse(rsp)
}

// GetArchiveInfoWithResponse What is known about the archive
//
// Reports the Ranke-Archive as a whole: the **branch-table head** — the id every
// archive-scoped read is rooted at — with its height and when it last moved, and how
// many branches the table holds.
//
// This is the only route that reports the branch-table head, which is what a client
// needs to name the `$archive` scope in a query or a grant.
//
// Reads the `$archive` scope and requires the **R** right on `$archive`. Cacheable
// **with revalidation**: the head advances on every contribution.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /archive/info (the `GetArchiveInfo` operationId).
func (c *ClientWithResponses) GetArchiveInfoWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetArchiveInfoResponse, error) {
	rsp, err := c.GetArchiveInfo(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetArchiveInfoResponse(rsp)
}

// ListBranchesWithResponse List the branch table's branches
//
// Returns every branch the branch table holds, each with its name and current
// head id. Reachable **without knowing any branch name**, so a client discovers
// what it may address before using the routes that take one; an archive with no
// branches yet answers with an empty list.
//
// Requires the **R** right on the reserved `$branches` target, which is what
// core-access grants as "enumerates the table".
//
// Cacheable **with revalidation** (weak `ETag`, `Cache-Control: no-cache`): every
// head in the list moves as its branch is contributed to. The listing is answered
// from **one archive snapshot**, so the heads are consistent with each other.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /branches (the `ListBranches` operationId).
func (c *ClientWithResponses) ListBranchesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBranchesResponse, error) {
	rsp, err := c.ListBranches(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBranchesResponse(rsp)
}

// GetBranchClaimWithResponse Fetch a claim within a branch's closure
//
// Returns claim `{id}` as its signed CBOR bytes, **only if it lies in branch
// `{branch}`'s closure**. A claim that is superseded, contradicted, or otherwise
// outside the closure returns `404`, indistinguishable from one that does not
// exist. Requires the **R** right on that branch.
//
// `{branch}` names an **ordinary branch**. A reserved scope name supplied here
// names a branch that does not exist and is answered as `404`; each scope has
// exactly one route (`/archive/…`, `/universe/…`).
//
// Immutably **cacheable** by id (strong `ETag`, `Cache-Control: public,
// immutable`): the id content-addresses the bytes, so they never change.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /branches/{branch}/claims/{id} (the `GetBranchClaim` operationId).
func (c *ClientWithResponses) GetBranchClaimWithResponse(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*GetBranchClaimResponse, error) {
	rsp, err := c.GetBranchClaim(ctx, branch, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBranchClaimResponse(rsp)
}

// GetBranchClaimContentWithResponse Fetch the content of a claim within a branch's closure
//
// Streams the content of claim `{id}` — **only if it lies in branch `{branch}`'s
// closure** (same closure guarantee as the claim itself; out-of-closure or
// unknown → `404`). Requires the **R** right on that branch. Content is addressed
// by the claim that holds it, not by a raw hash: the server resolves whether the
// bytes live inline in the claim or in a separate blob, so the client can't tell
// and doesn't need to — and the read stays scoped as the route is scoped.
// Immutably **cacheable** by the claim id.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /branches/{branch}/claims/{id}/content (the `GetBranchClaimContent` operationId).
func (c *ClientWithResponses) GetBranchClaimContentWithResponse(ctx context.Context, branch BranchName, id ClaimId, reqEditors ...RequestEditorFn) (*GetBranchClaimContentResponse, error) {
	rsp, err := c.GetBranchClaimContent(ctx, branch, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBranchClaimContentResponse(rsp)
}

// GetBranchHeadWithResponse Current head id of a branch
//
// Returns the branch's current head id — a moving target (it advances on every
// contribution). Requires the **R** right on that branch. Cacheable **with
// revalidation** (weak `ETag`, `Cache-Control: no-cache`): a conditional request
// is cheap when the head has not moved. To inspect the head claim itself, fetch it
// via `GET /branches/{branch}/claims/{id}`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /branches/{branch}/head (the `GetBranchHead` operationId).
func (c *ClientWithResponses) GetBranchHeadWithResponse(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*GetBranchHeadResponse, error) {
	rsp, err := c.GetBranchHead(ctx, branch, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBranchHeadResponse(rsp)
}

// GetBranchInfoWithResponse What is known about a branch
//
// Reports a branch beyond its head id: the head's **height** — the generation number
// of the branch's newest claim, so the depth of what it points at — and **when it last
// moved**, which is the head claim's `created_at`.
//
// Everything here comes from the head claim, so it costs one claim read. A claim count
// is deliberately absent: counting a branch's claims is a walk of its closure, which is
// a query (`POST /query`) and not a field.
//
// Requires the **R** right on the branch. Cacheable **with revalidation** — every
// field moves when the branch does.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /branches/{branch}/info (the `GetBranchInfo` operationId).
func (c *ClientWithResponses) GetBranchInfoWithResponse(ctx context.Context, branch BranchName, reqEditors ...RequestEditorFn) (*GetBranchInfoResponse, error) {
	rsp, err := c.GetBranchInfo(ctx, branch, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBranchInfoResponse(rsp)
}

// ContributeWithBodyWithResponse Contribute signed claims (atomic)
//
// Contributes one or more signed claims to a branch as a single **atomic**
// merge — all are absorbed under one new branch-table head, or none are.
// Content-addressed and therefore **idempotent**: re-contributing yields the same
// ids with no duplicates.
//
// The body is ranke's contribution stream: a **CBOR sequence** (RFC 8742) of
// records, self-framing so a contribution of any size streams.
//
//   - `[2, ["<branch>", …]]` — **first**, the branches this contribution touches
//   - `[0, <id>, <claim bytes>, "<branch>"]` — a claim, as the canonical CBOR its
//     contributor signed, and the branch it joins
//   - `[1, <hash>, <content bytes>]` — externalized content, which lives in the
//     Universe unbranched and so names no branch
//
// The header comes first so the **C** right is settled on every declared branch
// before any of the body is read; a claim naming an undeclared branch is refused, so
// the declaration binds. One contribution may therefore advance **several branches**,
// and an unauthorized one is answered without its payload being read.
//
// Payloads are CBOR byte strings carried through untouched: a claim's id is a
// signature over exactly those bytes, so the server stores what it was sent. The id
// is explicit because it cannot be derived — `Sign(H(S(node)))` is made with the
// contributor's key, and the canonical encoding carries only the node. Content is
// checked against the hash addressing it.
//
// Send every claim the closure needs that the archive may not hold yet — a
// first-time contributor includes its own `contribution/contributor` claim, since
// everything it signs references it.
//
// The sequencer mints the `contribution/branches` branch-table claim recording the
// merge (paper 02 §Sequencer). On success the new branch-table head id and the
// contributed claim ids are returned.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /contribute (the `Contribute` operationId).
func (c *ClientWithResponses) ContributeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ContributeResponse, error) {
	rsp, err := c.ContributeWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseContributeResponse(rsp)
}

// AdvanceDevClockWithBodyWithResponse Advance the dev sequencer's clock
//
// Available **only** when the stack was launched with `--dev` against a `dev`
// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
// timestamps from forward to (at least) the given instant, so a client that
// knows its own story's schedule — a fixture generator, say — can make the
// archive's *recorded* history track its *narrated* one, one contribution at a
// time, rather than every merge landing at the real wall-clock moment the
// client happened to run.
//
// The clock never moves backward: a request older than its current position
// is accepted and answered with the position unchanged. Absent `--dev`, or
// against a `concurrent` (production) Sequencer, the route is `501` — the
// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
func (c *ClientWithResponses) AdvanceDevClockWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AdvanceDevClockResponse, error) {
	rsp, err := c.AdvanceDevClockWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAdvanceDevClockResponse(rsp)
}

// AdvanceDevClockWithResponse Advance the dev sequencer's clock
//
// Available **only** when the stack was launched with `--dev` against a `dev`
// Sequencer: moves the clock the Sequencer mints `created_at` and branch-table
// timestamps from forward to (at least) the given instant, so a client that
// knows its own story's schedule — a fixture generator, say — can make the
// archive's *recorded* history track its *narrated* one, one contribution at a
// time, rather than every merge landing at the real wall-clock moment the
// client happened to run.
//
// The clock never moves backward: a request older than its current position
// is accepted and answered with the position unchanged. Absent `--dev`, or
// against a `concurrent` (production) Sequencer, the route is `501` — the
// witnessed merge time stays real, which is the whole point of `R-C2DATE`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /dev/clock (the `AdvanceDevClock` operationId).
func (c *ClientWithResponses) AdvanceDevClockWithResponse(ctx context.Context, body AdvanceDevClockJSONRequestBody, reqEditors ...RequestEditorFn) (*AdvanceDevClockResponse, error) {
	rsp, err := c.AdvanceDevClock(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAdvanceDevClockResponse(rsp)
}

// HealthWithResponse Liveness and signer identity
//
// Reports that the stack is up and the signing/contributor identity it
// attests merges under. Whether this requires a privileged subject is a
// core-access decision, not part of this contract.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /health (the `Health` operationId).
func (c *ClientWithResponses) HealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*HealthResponse, error) {
	rsp, err := c.Health(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseHealthResponse(rsp)
}

// QueryWithBodyWithResponse Read the graph with a declarative query
//
// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
// result, in the serialization chosen by `output.encoding`. Results carry the
// natural `(created_at, id)` order unless `order` sorts them, with its keys
// applied in priority order and that natural order breaking any remaining ties;
// to page, carry the last result's order key into the next request's `where`
// (pin `select.head` so the closure cannot shift between pages).
//
// When `execution.report` names a verbosity, the **final item** in the sequence
// is a `QueryReport` (see the schema) — typed distinctly from result items and
// always last — carrying the execution log, timing, and whether a limit truncated
// the read. `execution.layer` pins which storage layer runs it.
//
// The response media type mirrors `output.encoding`: `application/json-seq` or
// `application/cbor-seq`. A branch named in `select` that does not exist is
// `404`; claims outside the scope's closure simply do not appear. `$universe`
// is the privileged unconfined read and requires `select.head`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /query (the `Query` operationId).
func (c *ClientWithResponses) QueryWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*QueryResponse, error) {
	rsp, err := c.QueryWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseQueryResponse(rsp)
}

// QueryWithResponse Read the graph with a declarative query
//
// Runs a RankeQL query (§RankeQL) and streams the result set, one item per
// result, in the serialization chosen by `output.encoding`. Results carry the
// natural `(created_at, id)` order unless `order` sorts them, with its keys
// applied in priority order and that natural order breaking any remaining ties;
// to page, carry the last result's order key into the next request's `where`
// (pin `select.head` so the closure cannot shift between pages).
//
// When `execution.report` names a verbosity, the **final item** in the sequence
// is a `QueryReport` (see the schema) — typed distinctly from result items and
// always last — carrying the execution log, timing, and whether a limit truncated
// the read. `execution.layer` pins which storage layer runs it.
//
// The response media type mirrors `output.encoding`: `application/json-seq` or
// `application/cbor-seq`. A branch named in `select` that does not exist is
// `404`; claims outside the scope's closure simply do not appear. `$universe`
// is the privileged unconfined read and requires `select.head`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /query (the `Query` operationId).
func (c *ClientWithResponses) QueryWithResponse(ctx context.Context, body QueryJSONRequestBody, reqEditors ...RequestEditorFn) (*QueryResponse, error) {
	rsp, err := c.Query(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseQueryResponse(rsp)
}

// ListStorageLayersWithResponse List storage layers
//
// Lists the stack's storage layers (read-through tiers) by **name and type
// only** — never connection details or secrets. Naming layers is what lets a
// verification run target one directly (a read-through view can mask the loss
// of an object on a deeper layer).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /system/layers (the `ListStorageLayers` operationId).
func (c *ClientWithResponses) ListStorageLayersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListStorageLayersResponse, error) {
	rsp, err := c.ListStorageLayers(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListStorageLayersResponse(rsp)
}

// ListVerificationsWithResponse List verification runs
//
// Verification runs across the whole stack, newest first. Each report is a
// **point-in-time record**: a layer repaired externally shows clean in a later
// run, so reports accumulate rather than overwrite — until explicitly removed
// with `DELETE /system/verifications/{reportId}`.
//
// The API lives under `/system/` because a verification run is a ranke-db
// extension beyond the paper's surface rather than archive content: it is a
// stack-wide operational resource, rooted at a closure named in the request body
// and not in the path. (Branch reads no longer occupy the root, so nothing here
// is avoiding a collision.)
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /system/verifications (the `ListVerifications` operationId).
func (c *ClientWithResponses) ListVerificationsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListVerificationsResponse, error) {
	rsp, err := c.ListVerifications(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListVerificationsResponse(rsp)
}

// StartVerificationWithBodyWithResponse Start a verification run
//
// Starts a run from a `VerificationConfig`: walk the closure rooted at
// `closure` (a branch name — resolved to its current head and **pinned** for
// the life of the run — or a head id directly), reading the named `layer`
// directly, re-checking each claim to the configured depth. It returns
// *findings*, not contents, so it never leaks across closures.
//
// A run may take a **very long time** (hours to days over a large closure at
// full-content depth), so it is always **asynchronous**: the call returns `202`
// immediately with the running report and a `Location` header pointing at the
// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
// yields two independent point-in-time reports (runs are not deduplicated).
//
// Verification is resource-heavy, so the stack caps the number of runs that may
// execute **concurrently** (configured; default 1). When that cap is already
// reached the call returns `429` — the server never stops a run to make room. To
// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
// what is running and free a slot deliberately: `cancel` a run to stop it while
// keeping its report, or `DELETE` it to remove it entirely.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /system/verifications (the `StartVerification` operationId).
func (c *ClientWithResponses) StartVerificationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*StartVerificationResponse, error) {
	rsp, err := c.StartVerificationWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseStartVerificationResponse(rsp)
}

// StartVerificationWithResponse Start a verification run
//
// Starts a run from a `VerificationConfig`: walk the closure rooted at
// `closure` (a branch name — resolved to its current head and **pinned** for
// the life of the run — or a head id directly), reading the named `layer`
// directly, re-checking each claim to the configured depth. It returns
// *findings*, not contents, so it never leaks across closures.
//
// A run may take a **very long time** (hours to days over a large closure at
// full-content depth), so it is always **asynchronous**: the call returns `202`
// immediately with the running report and a `Location` header pointing at the
// report resource. Poll it with `GET /system/verifications/{reportId}`, pacing
// by the `Retry-After` hint; stop it with `DELETE`. Starting the same run twice
// yields two independent point-in-time reports (runs are not deduplicated).
//
// Verification is resource-heavy, so the stack caps the number of runs that may
// execute **concurrently** (configured; default 1). When that cap is already
// reached the call returns `429` — the server never stops a run to make room. To
// proceed, either wait (per `Retry-After`) or `GET /system/verifications` to see
// what is running and free a slot deliberately: `cancel` a run to stop it while
// keeping its report, or `DELETE` it to remove it entirely.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /system/verifications (the `StartVerification` operationId).
func (c *ClientWithResponses) StartVerificationWithResponse(ctx context.Context, body StartVerificationJSONRequestBody, reqEditors ...RequestEditorFn) (*StartVerificationResponse, error) {
	rsp, err := c.StartVerification(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseStartVerificationResponse(rsp)
}

// DeleteVerificationWithResponse Delete a verification run
//
// Really deletes the run and its report. If the run is still `running` it is
// stopped first, then the record is removed — so this both aborts a run and
// cleans up finished history in one step. Unlike the cancel action, nothing
// survives: a subsequent `GET` is `404`. Either verb frees a concurrency slot;
// cancel keeps the report, delete removes it.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /system/verifications/{reportId} (the `DeleteVerification` operationId).
func (c *ClientWithResponses) DeleteVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*DeleteVerificationResponse, error) {
	rsp, err := c.DeleteVerification(ctx, reportId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteVerificationResponse(rsp)
}

// GetVerificationWithResponse Show a verification run
//
// The report for a run. Poll until `status` leaves `running`; while it is still
// running the response carries a `Retry-After` hint and the progress counters
// (`claimsChecked`, `bytesRead`) advance between polls.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /system/verifications/{reportId} (the `GetVerification` operationId).
func (c *ClientWithResponses) GetVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*GetVerificationResponse, error) {
	rsp, err := c.GetVerification(ctx, reportId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetVerificationResponse(rsp)
}

// CancelVerificationWithResponse Cancel a running verification run
//
// Stops a `running` run but **keeps** its report: the record stays in history
// with `status` `stopped` and whatever partial findings it had gathered, and the
// concurrency slot is freed. Use this to abort a run you want to keep a record
// of; use `DELETE` to remove it entirely. Idempotent — cancelling a run that has
// already finished or stopped returns its current report unchanged.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /system/verifications/{reportId}/cancel (the `CancelVerification` operationId).
func (c *ClientWithResponses) CancelVerificationWithResponse(ctx context.Context, reportId string, reqEditors ...RequestEditorFn) (*CancelVerificationResponse, error) {
	rsp, err := c.CancelVerification(ctx, reportId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCancelVerificationResponse(rsp)
}

// WhoamiWithResponse What this credential may do
//
// Reports the caller back to itself: the account its credential resolved to,
// that account's grants, and any caveats attenuating them. It answers about
// **this server's** access policy, never about the graph, which is why it sits
// under `/system`.
//
// Needs no grant — a caller learns only what it already proved by
// authenticating, and nothing about any other account. A bad credential is
// still `401`, so this doubles as the side-effect-free way to check one.
//
// `grants` and `caveats` are reported separately rather than intersected: a
// request is allowed when the account's grants permit it **and** every caveat
// still does, so seeing both is what explains a refusal. A token narrowed by
// attenuation has no other way to show what survived.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /system/whoami (the `Whoami` operationId).
func (c *ClientWithResponses) WhoamiWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*WhoamiResponse, error) {
	rsp, err := c.Whoami(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseWhoamiResponse(rsp)
}

// GetClaimWithResponse Fetch a claim by id from the Universe (privileged)
//
// Returns claim `{id}` as its signed CBOR bytes from the Universe under **no
// closure at all** — no branch table, and no confinement to the current head.
// This is what makes it privileged, and what it exists for: reaching an archive
// from a Universe and a head id alone, as when restoring from a head kept outside
// the server. A claim the Universe holds is returned even where
// `GET /archive/claims/{id}` reports it not-found.
//
// This collection reads the `$universe` scope: the same scope a RankeQL body names
// as `select.branch: "$universe"`, and the same target a grant is written against.
// It requires the **R** right on `$universe`, to which only **R** applies. An
// ordinary glob confers it by no accident — a `$`-prefixed target needs an exact
// grant, so `R *` reaches neither reserved scope.
//
// Immutably cacheable by id.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /universe/claims/{id} (the `GetClaim` operationId).
func (c *ClientWithResponses) GetClaimWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetClaimResponse, error) {
	rsp, err := c.GetClaim(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetClaimResponse(rsp)
}

// GetClaimContentWithResponse Fetch the content of a claim by id from the Universe (privileged)
//
// Streams the content of claim `{id}` from the Universe under no closure — the
// privileged read, conferred only through **R** on `$universe`. Content is
// addressed by the claim; whether the bytes are inline or a separate blob is
// hidden. Immutably cacheable by the claim id.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /universe/claims/{id}/content (the `GetClaimContent` operationId).
func (c *ClientWithResponses) GetClaimContentWithResponse(ctx context.Context, id ClaimId, reqEditors ...RequestEditorFn) (*GetClaimContentResponse, error) {
	rsp, err := c.GetClaimContent(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetClaimContentResponse(rsp)
}

// ParseGetArchiveClaimResponse parses an HTTP response from a GetArchiveClaimWithResponse call
func ParseGetArchiveClaimResponse(rsp *http.Response) (*GetArchiveClaimResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetArchiveClaimResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetArchiveClaimResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetArchiveClaimContentResponse parses an HTTP response from a GetArchiveClaimContentWithResponse call
func ParseGetArchiveClaimContentResponse(rsp *http.Response) (*GetArchiveClaimContentResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetArchiveClaimContentResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetArchiveClaimContentResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetArchiveInfoResponse parses an HTTP response from a GetArchiveInfoWithResponse call
func ParseGetArchiveInfoResponse(rsp *http.Response) (*GetArchiveInfoResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetArchiveInfoResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ArchiveInfo
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetArchiveInfoResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseListBranchesResponse parses an HTTP response from a ListBranchesWithResponse call
func ParseListBranchesResponse(rsp *http.Response) (*ListBranchesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBranchesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BranchList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers ListBranchesResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetBranchClaimResponse parses an HTTP response from a GetBranchClaimWithResponse call
func ParseGetBranchClaimResponse(rsp *http.Response) (*GetBranchClaimResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBranchClaimResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetBranchClaimResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetBranchClaimContentResponse parses an HTTP response from a GetBranchClaimContentWithResponse call
func ParseGetBranchClaimContentResponse(rsp *http.Response) (*GetBranchClaimContentResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBranchClaimContentResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetBranchClaimContentResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetBranchHeadResponse parses an HTTP response from a GetBranchHeadWithResponse call
func ParseGetBranchHeadResponse(rsp *http.Response) (*GetBranchHeadResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBranchHeadResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BranchHead
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetBranchHeadResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetBranchInfoResponse parses an HTTP response from a GetBranchInfoWithResponse call
func ParseGetBranchInfoResponse(rsp *http.Response) (*GetBranchInfoResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBranchInfoResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BranchInfo
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetBranchInfoResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseContributeResponse parses an HTTP response from a ContributeWithResponse call
func ParseContributeResponse(rsp *http.Response) (*ContributeResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ContributeResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ContributionResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	}

	return response, nil
}

// ParseAdvanceDevClockResponse parses an HTTP response from a AdvanceDevClockWithResponse call
func ParseAdvanceDevClockResponse(rsp *http.Response) (*AdvanceDevClockResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AdvanceDevClockResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest DevClock
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 501:
		var dest NotImplemented
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON501 = &dest

	}

	return response, nil
}

// ParseHealthResponse parses an HTTP response from a HealthWithResponse call
func ParseHealthResponse(rsp *http.Response) (*HealthResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &HealthResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Health
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseQueryResponse parses an HTTP response from a QueryWithResponse call
func ParseQueryResponse(rsp *http.Response) (*QueryResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &QueryResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseListStorageLayersResponse parses an HTTP response from a ListStorageLayersWithResponse call
func ParseListStorageLayersResponse(rsp *http.Response) (*ListStorageLayersResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListStorageLayersResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest StorageLayerList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseListVerificationsResponse parses an HTTP response from a ListVerificationsWithResponse call
func ParseListVerificationsResponse(rsp *http.Response) (*ListVerificationsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListVerificationsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest VerificationReportList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseStartVerificationResponse parses an HTTP response from a StartVerificationWithResponse call
func ParseStartVerificationResponse(rsp *http.Response) (*StartVerificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &StartVerificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest VerificationReport
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	}

	switch {
	case rsp.StatusCode == 202:
		var headers StartVerificationResponse202Headers
		if values := rsp.Header.Values("Location"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "Location", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.Location = &value
		}
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers202 = &headers
	case rsp.StatusCode == 429:
		var headers StartVerificationResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseDeleteVerificationResponse parses an HTTP response from a DeleteVerificationWithResponse call
func ParseDeleteVerificationResponse(rsp *http.Response) (*DeleteVerificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteVerificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetVerificationResponse parses an HTTP response from a GetVerificationWithResponse call
func ParseGetVerificationResponse(rsp *http.Response) (*GetVerificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetVerificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest VerificationReport
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetVerificationResponse200Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseCancelVerificationResponse parses an HTTP response from a CancelVerificationWithResponse call
func ParseCancelVerificationResponse(rsp *http.Response) (*CancelVerificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CancelVerificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest VerificationReport
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseWhoamiResponse parses an HTTP response from a WhoamiWithResponse call
func ParseWhoamiResponse(rsp *http.Response) (*WhoamiResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &WhoamiResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Subject
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetClaimResponse parses an HTTP response from a GetClaimWithResponse call
func ParseGetClaimResponse(rsp *http.Response) (*GetClaimResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetClaimResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetClaimResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}

// ParseGetClaimContentResponse parses an HTTP response from a GetClaimContentWithResponse call
func ParseGetClaimContentResponse(rsp *http.Response) (*GetClaimContentResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetClaimContentResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetClaimContentResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	}

	return response, nil
}
